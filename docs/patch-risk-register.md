# Patch Risk Register

Binary patching is high risk. This register records known patch areas and
verification commands; it does not authorize more patching.

Generated evidence:
[generated/code-audit/patch-related-files.txt](generated/code-audit/patch-related-files.txt)
and
[generated/code-audit/binary-patch-sites.tsv](generated/code-audit/binary-patch-sites.tsv).

| Patch/Area | Target Binary | Offset/Symbol | Purpose | Current Evidence | Risk | Verification Command |
| --- | --- | ---: | --- | --- | --- | --- |
| Shandalar DIB `hSection = NULL` | `Shandalar.exe`; `Program/Shandalar.exe` | `0x1785b0` file offset | Avoid start-color `CreateDIBSection` crash path | documented bytes and hashes in AGENTS/bugs docs | high | Run `xxd -g1 -l 32 -s $((0x1785b0))` separately for each target. |
| Shandalar name seed | `Shandalar.exe`; `Program/Shandalar.exe` | `0xa1a42` | Seed default player name | documented bytes/hashes | high | AGENTS name-entry `xxd` command |
| Shandalar name-editor bypass/fallback | `Shandalar.exe`; `Program/Shandalar.exe` | `0xa1acd`; `0xa1af2`; cave `0x465170` | Avoid fragile manual name editor and empty-name copy | documented bytes/hashes | high | AGENTS bypass/fallback `xxd` commands |
| Shandalar same-arrow movement stop | `Shandalar.exe`; `Program/Shandalar.exe` | hooks `0x44398c`, `0x444a2b`, `0x444aa7`; cave `0x46502d` | Allow repeated direction key to stop map movement | documented disassembly expectation | high | AGENTS `lldb` disassembly command |
| Shandalar MagSnd update-message compatibility | `Shandalar.exe`; `Program/Shandalar.exe` | VA `0x4ce62e`; file offset `0xcda2e` | Avoid the shell/window private `0x10101010` MagSnd message calling through the legacy `UpdateSnd` function pointer at `0x73bfe0` | Wine debugger page fault with `EIP == EDX == 0xfff50de4`, static caller analysis showing the remaining `0x56d476` caller after the WinMM timer patch, guarded patch helper, byte/hash checks, and local `MTG` copied-install hashes | high | `python3 tools/patch-shandalar-magsnd-update-callback.py`; `xxd -p -l 5 -s $((0xcda2e)) Shandalar.exe Program/Shandalar.exe` |
| Shandalar WinMM timer-callback compatibility | `Shandalar.exe`; `Program/Shandalar.exe` | VA `0x4ce93f`; file offset `0xcdd3f` | Preserve the 33 ms tick counter while avoiding a callback-thread call to legacy sound/service dispatcher `0x56d476` | Wine debugger page fault with `EIP == EDX == 0xfff50de4`, static `timeSetEvent()` callback analysis, guarded patch helper, byte/hash checks, and local `MTG` copied-install hashes | high | `python3 tools/patch-shandalar-winmm-tick-callback.py`; AGENTS WinMM timer-callback `xxd` command |
| Shandalar minimal WinMM timer callback | `Shandalar.exe`; `Program/Shandalar.exe` | VA `0x4ce8cd`; file offset `0xcdccd` | Keep the timer tick counter alive while removing first-run thread-handle and priority bookkeeping from Wine's timer worker thread | Priestess pre-card freeze still produced `EIP == EDX == 0xfff50de4` after live inspection showed `MagSnd.dll` and `magvid.dll` absent, leaving Shandalar's own `timeSetEvent()` callback as the remaining recurring async callback path | high | `python3 tools/patch-shandalar-minimal-winmm-timer-callback.py`; `xxd -p -l 16 -s $((0xcdccd)) Shandalar.exe Program/Shandalar.exe` |
| Shandalar MCIWndCreateA disable | `Shandalar.exe`; `Program/Shandalar.exe` | VA `0x578c10`; file offset `0x178010` | Avoid entering Wine's legacy MSVFW32 MCI window/video path after the known MagSnd, MagVid, WinMM timer, and Drawcardlib GDI+ worker-thread paths were mitigated | Later priestess pre-card freeze happened with the current patched bottle, no `Magic.exe`, `MagSnd.dll`, `magvid.dll`, or `ManalinkEh.dll` loaded, but MSVFW32 still loaded and Wine debugger again reported `EIP == EDX == 0xfff50de4`; static analysis found exactly two `MCIWndCreateA` callers and both perform caller-side `add esp, 0x10` cleanup | high | `python3 tools/patch-shandalar-disable-mciwndcreate.py`; `xxd -p -l 6 -s $((0x178010)) Shandalar.exe Program/Shandalar.exe` |
| Drawcardlib GDI+ background-thread suppression | `Drawcardlib.dll`; `Program/Drawcardlib.dll`; `src/drawcardlib/drawcardlib.c`; `Program/src/drawcardlib/drawcardlib.c`; `src/drawcardlib/Makefile`; `Program/src/drawcardlib/Makefile` | Source rebuild | Avoid Wine/GDI+ creating an internal background thread during Spell Chain card rendering; Drawcardlib now uses explicit GDI+ notification hook/unhook handling like CardArtLib while retaining legacy-safe PE startup flags | Thorn Thallid Spell Chain freeze still faulted at `EIP == EDX == 0xfff50de4` with `MagSnd.dll` and `magvid.dll` absent; live `lsof` showed `Thorn Thallid.jpg` open and loaded modules included Drawcardlib/CardArtLib/GDI+; macOS sampling showed the UI thread idle while a Wine thread hit the invalid callback path. The first GDI+ rebuild regressed CrossOver startup because `DrawCardLib.dll` had `DYNAMIC_BASE`/`NX_COMPAT`; the accepted rebuild has PE `DllCharacteristics` `0x0000`. | high | `PATH=/opt/homebrew/Cellar/mingw-w64/14.0.0/toolchain-i686/bin:/opt/homebrew/Cellar/mingw-w64/14.0.0/toolchain-i686/i686-w64-mingw32/bin:/opt/homebrew/bin:$PATH make -C src/drawcardlib CC=i686-w64-mingw32-gcc CFLAGS='-ggdb2 -O2 -m32 -fcommon -fno-omit-frame-pointer -march=i586 -mpreferred-stack-boundary=2 -Wall -Wextra -Wpointer-arith -Wbad-function-cast -Wcast-qual -Wcast-align -Wwrite-strings -Wshadow -W -Wstrict-prototypes -Wnested-externs -Winline -Werror -Wno-error -I.. -I.'`; `shasum -a 256 Drawcardlib.dll Program/Drawcardlib.dll`; `tools/verify-install-tree.sh .` |
| Shandalar MagSnd initialization disable | `Shandalar.exe`; `Program/Shandalar.exe` | VA `0x56cf20`; file offset `0x16c320` | Avoid initializing `MagSnd.dll` in the adventure executable after recurring CrossOver page faults persisted below the previous `UpdateSnd` and WinMM call-site patches; later sound wrappers return unavailable | Thoughtseize Spell Chain freeze with corrupted stack render, Wine debugger `EIP == EDX == 0xfff50de4`, live process loading `MagSnd.dll`, static init-wrapper analysis, guarded patch helper, byte/hash checks, and local `MTG` copied-install hashes | high | `python3 tools/patch-shandalar-disable-magsnd-init.py`; `xxd -p -l 6 -s $((0x16c320)) Shandalar.exe Program/Shandalar.exe` |
| Statwin MagVid loader disable | `Statwin.dll`; `Program/Statwin.dll` | VA `0x10003610`; file offset `0x2a10` | Avoid loading `magvid.dll` and its worker-thread/callback path after Augur still froze with `MagSnd.dll` absent | Augur `Witch activates...` freeze after the MagSnd init patch, Wine debugger `EIP == EDX == 0xfff50de4`, `lsof` showing `MagSnd.dll` absent while `Statwin.dll`/`magvid.dll` were loaded, static dynamic-loader and MagVid worker-thread/callback analysis, guarded patch helper, byte/hash checks, and local `MTG` copied-install hashes | high | `python3 tools/patch-statwin-disable-magvid-loader.py`; `xxd -p -l 6 -s $((0x2a10)) Statwin.dll Program/Statwin.dll` |
| FaceMaker DIB `hSection = NULL` | `FaceMaker.exe`; `Program/FaceMaker.exe` | `0x5f40` | Avoid helper-side DIB assertion path | documented bytes/hashes | high | Run `xxd -g1 -l 32 -s $((0x5f40))` separately for each target. |
| Damage-prevention healer guard | `ManalinkEh.dll`; `Program/ManalinkEh.dll` | `0x3bb035`; `0x381a25` | Gate Samite/Femeref/Kithkin activation on damage-prevention window | documented bytes/hashes; manual retest pending | high | AGENTS healer `xxd` and `shasum` commands |
| Generic activated damage-prevention guard | `ManalinkEh.dll`; `Program/ManalinkEh.dll` | jumps at `0x44cb23`/`0x40f115`; caves at `0x495a30`/`0x452c30`; section virtual-size header `0x1a8` now `0x200` | Gate all generic activated `GAA_DAMAGE_PREVENTION*` can-activate prompts on `LCBP_DAMAGE_PREVENTION` | documented branch/cave bytes and hashes; manual retest pending | high | AGENTS ManalinkEh damage-prevention commands |
| AI decision-time clamp | `ManalinkEh.dll`; `Program/ManalinkEh.dll` | jumps at `0x40d0e1`/`0x3d2da1`; caves at `0x495a60`/`0x452c60`; section virtual-size header `0x1a8` now `0x200` | Keep `AiDecisionTime` in the fast `1..270` range, using 270 for missing, invalid, or higher configured values | documented hook/cave bytes and hashes; manual opponent-turn retest pending | high | AGENTS AI clamp `xxd` and `objdump` commands |
| AI raw-mana speculation snapshot | `ManalinkEh.dll`; `Program/ManalinkEh.dll` | jumps at `0x40db84`/`0x3d3844`; caves at `0x495a90`/`0x452c90`; section virtual-size header `0x1a8` now `0x200` | Save both players' `raw_mana_available` rows before AI speculation temporarily replaces the opponent row | documented source fix, hook/cave bytes, repo hashes, and local `MTG` copied-install hashes | high | AGENTS raw-mana snapshot `xxd` and `objdump` commands |
| Piranha Marsh trigger target | `ManalinkEh.dll`; `Program/ManalinkEh.dll` | inline replacements at `0x3fe7a0`/`0x3c4930` | Route Piranha Marsh's ETB trigger through `pick_player_duh()` so AI/Duh mode directly targets the opponent while human choice is preserved | documented source fix, patch helper, byte/hash checks, and local `MTG` copied-install hashes; manual Piranha Marsh retest pending | high | `python3 tools/patch-piranha-marsh-trigger-target.py`; AGENTS Piranha Marsh `xxd` commands |
| Bojuka Bog trigger target | `ManalinkEh.dll`; `Program/ManalinkEh.dll` | inline replacements at `0x3f63e0`/`0x3bc630` | Route Bojuka Bog's ETB trigger through `pick_player_duh()` so AI/Duh mode directly targets the opponent while human choice is preserved | documented source fix, patch helper, byte/hash checks, and local `MTG` copied-install hashes; manual Bojuka Bog retest failed after this selector-side mitigation | high | `python3 tools/patch-bojuka-bog-trigger-target.py`; AGENTS Bojuka Bog `xxd` commands |
| Generic AI player-target selector | `ManalinkEh.dll`; `Program/ManalinkEh.dll`; `Shandalar.dll`; `Program/Shandalar.dll` | ManalinkEh hooks at `0x469583`/`0x429453`; caves at `0x495ad0`/`0x452cd0`; Shandalar C++ targeter hook/cave at `0xcb16`/`0x1174920` | Skip the generic selector for AI-controlled player-only targets after the candidate list has already been filtered by `preferred_controller` | documented source fix, Shandalar targeter follow-up after manual Augur freeze, patch helper, byte/hash checks, and local `MTG` copied-install hashes; fresh Augur/Piranha/Bojuka manual retest pending | high | `python3 tools/patch-ai-player-target-selection.py`; AGENTS AI player-target `xxd` commands |
| AI land CIP resolver stack-bypass handling | `ManalinkEh.dll`; `Program/ManalinkEh.dll`; `Shandalar.dll`; `Program/Shandalar.dll` | ManalinkEh resolver hooks at `0x429acf` and `0x3ec7cf`; caves at `0x495b00` and `0x452d00`; restored Piranha/Bojuka callsites at `0x3fe77d`/`0x3f63bd` and `0x3c490d`/`0x3bc60d`; Shandalar resolver hook `.cdxai` at `0x94d34`/`0x1174800` in root and Program | For non-speculating AI-owned land `TRIGGER_COMES_INTO_PLAY` contexts, dispatch `EVENT_RESOLVE_TRIGGER` directly before `put_card_or_activation_onto_stack()` opens Spell Chain, then restore trigger globals, clear processing flags, and dispatch `EVENT_TRIGGER_RESOLVED`; all other resolver contexts fall back to the original path | documented source fix, patch helper, byte/hash checks, and local `MTG` copied-install hashes; manual Piranha/Bojuka retest required after the Manalink-only patch did not affect Shandalar adventure duels | high | `python3 tools/patch-ai-land-cip-trigger-stack-bypass.py --apply`; AGENTS AI land CIP resolver `xxd` commands |
| Declared-attacker undo | `Magic.exe`; `Program/Magic.exe` | hook `0x43c303`; cave `0x459bc8` | Let user clear `STATE_ATTACKING` before Done | documented bytes/hashes; manual edge-case retest pending | high | AGENTS attacker undo `xxd` and `shasum` commands |
| Magic coin-flip animation default and startup call-site guard | `Magic.exe`; `Program/Magic.exe` | `0x5db1f`; startup call sites `0x694b7`, `0x694eb` | Keep missing `ShowCoinFlips` registry value defaulted to off, and verify startup `coin_flip()` calls keep dialog-if-animation-off at `0` | documented bytes/hashes; static call-site bytes; manual duel-start retest pending | high | AGENTS coin-flip default `xxd`, startup call-site `xxd`, and `user.reg` commands |
| Patch scripts | `src/patches/*`; `Program/src/patches/*` | many offsets | Historical and current binary patch tooling | static inventory only | high | Read script, hash target, backup target, dry-run plan first |

## Do Not Patch Unless

All of these are true: target binary is identified, current SHA-256 is recorded,
expected bytes are verified, backup and rollback bytes exist, test plan exists,
and the user explicitly asks for the binary patch.
