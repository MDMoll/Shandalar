# Verified on This Machine

This file keeps the detailed local command/result evidence out of the README.
It records what was actually checked on this checkout; it is not a substitute
for manual gameplay testing.

Local checkout path: `/Users/mdmoll/Shandalar/Shandalar`

## Command Evidence

| Check | Result |
| --- | --- |
| `file Program/Shandalar.exe Program/Magic.exe` | Both are `PE32 executable (GUI) Intel 80386, for MS Windows`. |
| `objdump -p Program/Shandalar.exe` | Imports Win32 DLLs plus `DrawCardLib.dll`, `DECKDLL.dll`, `CdTools.dll`, `CardArtLib.dll`, and `MSVCRT.dll`. |
| `objdump -p Program/Magic.exe` | Imports Win32 DLLs plus `deckdll.dll`, `drawcardlib.dll`, `manalinkeh.dll`, and `manalinkex.dll`. |
| `shasum -a 256 FaceMaker.exe Program/FaceMaker.exe FaceMaker-nores.exe Program/FaceMaker-nores.exe` | Active `FaceMaker.exe` copies hash to `41f062874f94d732cc4feb40b568728b8462879fd3ec2bc55810f118e9c5f246`; no-resolution/Korath copies hash to `43331d22d05787979af0d29cea1775fd3bcebf8acdb3c3be34524e9ca7762f4b`. A later visible S2 run logged `FaceMaker-nores.exe /S`, so the no-resolution helper is preserved as runtime-relevant evidence, not only as a reference. |
| Separate `xxd -g1 -l 32 -s $((0x5f40))` checks for `FaceMaker.exe` and `Program/FaceMaker.exe` | Both active FaceMaker dumps begin `6a 00 57 50 8b 4d 10 51 ff 75 04`, forcing their `CreateDIBSection` wrapper to receive `hSection = NULL`. |
| `cmp -s /private/tmp/FaceMaker-Korath-thread.exe FaceMaker-nores.exe` plus `find /Users/mdmoll/Shandalar -iname '*facemaker*korath*'` | The known downloaded thread helper matches `FaceMaker-nores.exe`; no repo file named `Facemaker-Korath.exe` was visible under `/Users/mdmoll/Shandalar` during this pass. |
| `diff -qr Program/FaceArt Manalink3/Program/FaceArt` | No differences; FaceMaker art support is present in `Program/`. |
| `shasum -a 256 Shandalar.exe Program/Shandalar.exe` | Root and `Program/` `Shandalar.exe` match: `ebba01ad04aba5fb78841f37b6c264dfd17f1d6ca6ccfcc9851c2972b64f5f6b`. |
| `shasum -a 256 zlib.dll Program/zlib.dll` | Root and `Program/` `zlib.dll` match: `9f8729ac49e0ccea86fe3b1a9b2c3fae9986ecd09db92853e7a588dbda85bf90`. This is static repo-layout evidence only; the copied `MTG` Program path has not been visibly retested after the later Program config/font, CardArt, and card-data copies. |
| `shasum -a 256 "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/drive_c/Shandalar/zlib.dll" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/drive_c/Shandalar/Program/zlib.dll"` | The local `MTG` copied install also has matching root and Program zlib DLLs at `9f8729ac49e0ccea86fe3b1a9b2c3fae9986ecd09db92853e7a588dbda85bf90`. This is static copied-install evidence, not a visible Program-path launch result. |
| `shasum -a 256 Shandalar.dll Program/Shandalar.dll CardArtLib.dll Program/CardArtLib.dll DeckDLL.dll Program/Deckdll.dll Drawcardlib.dll Program/Drawcardlib.dll libgcc_s_dw2-1.dll Program/libgcc_s_dw2-1.dll` | Root and `Program/` helper pairs match: `Shandalar.dll` `f74648745315163da15ffbe32e5bbdbc79e05aaf47c0714902c8d6898e5d00f7`; rebuilt `CardArtLib.dll` `c1a68591059ff3e650104bf711d4e3f0c9a01a232db2e594af64aaa6846b3c1d`; rebuilt `DeckDLL.dll`/`Program/Deckdll.dll` `5c122ea5442d209d0d74c7e75f7b1f53492b0bfcc042efce49300f3485e3fcb0`; `Drawcardlib.dll` `24fbf84f26dd8258cd9ae23350971080fa2f192a210f610ca6a135b669b6ead9`; `libgcc_s_dw2-1.dll` `89f6147f5ed3f271d0b88f0586e079b9ac22e76c31221e5d5013aa273cc4694b`. |
| `shasum -a 256 Statwin.dll Program/Statwin.dll` plus `xxd -p -l 6 -s $((0x2a10))` on both files | Root and `Program/` `Statwin.dll` match at `f1428cf548810f85df6f26b913d10dca16bc0f06a609a94c0cb0f0308347b0cf`; both dumps are `b807000000c3`, making the dynamic MagVid loader return video-unavailable before it loads `magvid.dll`. This follows Augur freeze evidence where `MagSnd.dll` was absent but `Statwin.dll`/`magvid.dll` were loaded; it is static/bottle evidence, not manual gameplay proof. |
| `shasum -a 256` on local `MTG` copied-install root/Program helper DLL pairs and `libgcc_s_dw2-1.dll` | The local copied install also has matching root and Program helper hashes for `Shandalar.dll`, rebuilt `CardArtLib.dll`, `DeckDLL.dll`/`Deckdll.dll`, `Drawcardlib.dll`, and `libgcc_s_dw2-1.dll`. The current rebuilt CardArtLib hash is `c1a68591059ff3e650104bf711d4e3f0c9a01a232db2e594af64aaa6846b3c1d`; the current rebuilt Drawcardlib hash is `24fbf84f26dd8258cd9ae23350971080fa2f192a210f610ca6a135b669b6ead9`. Both rebuilt renderers suppress GDI+ external codecs and keep legacy-safe PE flags; Drawcardlib also uses checked wrappers for high-frequency GDI+ render operations. Neither rebuilt renderer imports `libgcc_s_dw2-1.dll` directly; that adjacent helper remains preserved after the earlier Program-path loader fix. |
| `objdump -p CardArtLib.dll Program/CardArtLib.dll Drawcardlib.dll Program/Drawcardlib.dll` and matching local `MTG` copied-install files | Root, Program, and copied-install renderer builds report PE `DllCharacteristics` `00000000`, confirming `DYNAMIC_BASE` and `NX_COMPAT` are disabled after the first GDI+ rebuild crashed during CrossOver `DrawCardLib.dll` process attach. |
| `shasum -a 256` and `xxd -p -l 6 -s $((0x2a10))` on local `MTG` copied-install root/Program `Statwin.dll` | The local copied install also has matching root and Program `Statwin.dll` hashes at `f1428cf548810f85df6f26b913d10dca16bc0f06a609a94c0cb0f0308347b0cf` and matching MagVid loader-disable bytes `b807000000c3`; backups were preserved as `*.before-magvid-loader-disable-patch.dll`. |
| `shasum -a 256 Cards.dat Program/Cards.dat DBInfo.dat Program/DBInfo.dat Rarity.dat Program/Rarity.dat` plus `xxd -e -g4 -l 4` on each | Root and `Program/` `Cards.dat` match at `abb2f631bd7897dcebde9d6c4bf61a6ea2e37e30fda42490c37b6f4d60f42e94`; root and `Program/` `DBInfo.dat` match at `519ccecb98548c1a2e15fe8025951aafba9f116595b5775a0f2ab2bb393e48c1`; root and `Program/` `Rarity.dat` match at `e0c779a73f0ed780b0c689741805a4e40f7f4949420a8d27fa73137e528ae04f`. All six headers now report `0x41b2` / 16818 records for the trio. |
| `shasum -a 256` on local `MTG` copied-install root/Program `Cards.dat`, `DBInfo.dat`, and `Rarity.dat` | The local copied install also has matching root and Program card-data hashes. This replaced the older Program data files after a visible `raw_cards_data[-1]` Hornet fatal and then DeckDLL's generic `Cards.dat, DBInfo.dat or Rarity.dat` fatal exposed mismatched 15718-record Program data. |
| `shasum -a 256 Manalink.ini Program/Manalink.ini Program/DuelArt/Modern.dat Program/DuelArt/Planeswalker.dat Program/TT*.ttf` | Active root and `Program/` `Manalink.ini` match at `30153fd22c76b0c0751c538938af46fbf25b1b51d5b4bb2bd9a2eead1b9c2f2b`; `Program/DuelArt/Modern.dat` hashes to `9a2d70be70b70ef27036a47550bc0d549437df0c032a4e0237a217e4731e1aee`; `Program/DuelArt/Planeswalker.dat` hashes to `619e0b9780ec204b9fbf6f48b2eb541c9d8a6f19a73f27d4d76d25828db7d369`; Program font hashes are recorded in [runtime-manifest.md](runtime-manifest.md). |
| `shasum -a 256` on local `MTG` copied-install `Program/Manalink.ini`, Program `DuelArt` configs, and Program `TT*.ttf` files | The local copied install has matching hashes for the adjacent Program config/font files recorded in [runtime-manifest.md](runtime-manifest.md). |
| `shasum -a 256` on the Program CardArt files listed in [runtime-manifest.md](runtime-manifest.md) | Program drawcardlib/CardArt assets are present in the repo through `Modern/CardOv_Nyx.png`, with exact hashes recorded in the runtime manifest. |
| `shasum -a 256` on the local `MTG` copied-install Program CardArt files listed in [runtime-manifest.md](runtime-manifest.md) | The local `MTG` copied install has matching Program CardArt hashes through `Modern/CardOv_Nyx.png`. This is static copied-install evidence after the visible and logged `drawcardlib.dll` missing-asset findings, not a visible Program-path success result. |
| `xxd -g1 -l 32 -s $((0x1785b0)) Shandalar.exe` and `Program/Shandalar.exe` | Both dumps begin `6a 00 57 50 8b 4d 10 51 ff 75 04`, forcing the Shandalar `CreateDIBSection` wrapper to receive `hSection = NULL`. |
| `xxd -g1 -l 40 -s $((0xa1a42)) Shandalar.exe` and `Program/Shandalar.exe` | Both dumps seed `mPlayer` at `0x591228`, avoiding a Wine-fragile pixel-copy read from the name picker surface before the existing code strips the leading gender byte. |
| `xxd -g1 -l 32 -s $((0xa1acd)) Shandalar.exe` and `Program/Shandalar.exe` | Both dumps begin `31 c0 89 85 a8 fe ff ff`, bypassing the fragile name editor and continuing with the seeded default name. |
| `xxd -g1 -l 64 -s $((0x64570)) Shandalar.exe` and `Program/Shandalar.exe` | Both dumps contain a fallback guard that writes `Player` if the accepted name buffer at `0x591228` is empty before copying it to `0x7a0770`. |
| `shasum -a 256 Magic.exe Program/Magic.exe` | Root and `Program/` `Magic.exe` differ after the declared-attacker undo and coin-flip default patches: `93a40ce2c96aafee1d858a71ed69eb8c539aa9851796eb54b1af58f0bb97aba0` and `685669692634ec830fe228904e11b1b536bd4b20e52192863a6280c2dbff6b66`. |
| Separate `xxd` checks at `0x43c303` and `0x459bc8` for `Magic.exe` and `Program/Magic.exe` | Both `Magic.exe` copies have the declared-attacker undo hook. The hook begins `e9 c0 d8 01 00 90 90 90 90 90 90 90 90`; the cave begins `f7 46 08 04 00 00 00 0f 85 12 00 00 00`. |
| Separate `xxd -g1 -l 10 -s $((0x5db1f))` checks for `Magic.exe` and `Program/Magic.exe` | Both patched dumps begin `c7 05 5c 72 78 00 00 00 00 00`, so missing `ShowCoinFlips` registry data defaults to `0` instead of `1`. |
| Bounded logged root direct-duel launch: `/usr/bin/perl -e 'alarm 25; exec @ARGV' /Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" --cx-log /tmp/shandalar-mtg-root-ep0442-coinflip-default-cx.log --debugmsg +seh,+file,+process "C:\Shandalar\Shandalar.exe" --e 0442 --p 0442` | The command printed `Stand-alone duel: "decks/0442.dck" vs. "decks/0442.dck"`. The log opened root `C:\Shandalar\Magic.exe`, loaded AVI support, and opened `statwin\water.avi`. Targeted scans found no `COINTOSS`, `ShowCoinFlips`, `DIALOG_STARTCOINFLIP`, `DIALOG_COINFLIP`, Hornet/card-data fatal, page fault, DIB assertion, or unhandled-exception strings; the only failure-scan hits were missing Wine audio-driver warnings. The child stayed alive past the alarm and was killed manually, so this is bounded log evidence only. |
| Bounded logged root direct-duel launch for the summoned undead/dark knight path: `/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" --cx-log /tmp/shandalar-mtg-root-ep0016-dark-knight-cx.log --debugmsg +seh,+file,+process "C:\Shandalar\Shandalar.exe" --e 0016 --p 0016` | The command printed `Stand-alone duel: "decks/0016.dck" vs. "decks/0016.dck"`. The log opened root `C:\Shandalar\Magic.exe` and opened `C:\Shandalar\decks\0016.dck` after normal fallback misses for bare `0016` paths. Targeted scans found no Hornet/card-data fatal, page fault, DIB assertion, unhandled-exception, `COINTOSS`, `ShowCoinFlips`, or `DIALOG_*COINFLIP` strings. The process stayed alive until killed manually; the cleanup tail includes `wineserver crashed`, so this is bounded handoff/crash-path evidence only, not visible first-turn proof. |
| `shasum -a 256 ManalinkEh.dll Program/ManalinkEh.dll` | Root patched hash is `0a2d77aa15fd18648a99398d7bb45b97e47a1a8ea6c01dc3e22433851940a507`; `Program/` patched hash is `e715b92495677abf940b3bfda438477d66896532b24c1b05a4ea2bc2179c2e22`. |
| `xxd -g1 -l 32 -s $((0x3bb035)) ManalinkEh.dll` and `xxd -g1 -l 32 -s $((0x381a25)) Program/ManalinkEh.dll` | Both patched dumps begin `f6 05 90 f1 4e 00 04 0f 84 ae 00 00 00 e9 01 00`, gating the Samite/Femeref/Kithkin healer handler on the engine's damage-prevention window. |
| `xxd -g1 -l 5 -s $((0x44cb23)) ManalinkEh.dll`; `xxd -g1 -l 38 -s $((0x495a30)) ManalinkEh.dll`; matching Program offsets `0x40f115` and `0x452c30` | The generic activated damage-prevention helper jumps into an executable cave that tests `GAA_DAMAGE_PREVENTION*` and `LCBP_DAMAGE_PREVENTION`, then replays the original `GAA_CAN_TARGET` branch. |
| `xxd` checks at `0x40d0e1`/`0x495a60` for root `ManalinkEh.dll` and `0x3d2da1`/`0x452c60` for `Program/ManalinkEh.dll` | Both DLLs hook `_check_timer_for_ai_speculation` into a cave that preserves configured values `1..270` and writes `270` for missing, invalid, or higher `AiDecisionTime` values. |
| `xxd` checks at `0x40db84`/`0x495a90` for root `ManalinkEh.dll` and `0x3d3844`/`0x452c90` for `Program/ManalinkEh.dll` | Both DLLs hook the `_ai_decision_phase` raw-mana snapshot block into a cave that saves all 16 `raw_mana_available` dwords before temporarily replacing only the opponent row during AI speculation. |
| `xxd -p -l 80 -s $((0xb9d40)) ManalinkEh.dll`; `xxd -p -l 80 -s $((0xb1cd0)) Program/ManalinkEh.dll` | Myr Moonvessel's function now calls `_this_dies_trigger` with trigger mode `2`, then calls `_produce_mana` only after that mandatory dies trigger resolves. |
| `xxd -p -l 75 -s $((0x3fe7a0)) ManalinkEh.dll`; `xxd -p -l 75 -s $((0x3c4930)) Program/ManalinkEh.dll` | Piranha Marsh's ETB trigger uses `pick_player_duh()` in the patched runtime path, so AI/Duh mode directly targets the opponent and avoids the generic player-target prompt. |
| `xxd -p -l 117 -s $((0x3f63e0)) ManalinkEh.dll`; `xxd -p -l 117 -s $((0x3bc630)) Program/ManalinkEh.dll` | Bojuka Bog's resolving ETB trigger uses `pick_player_duh()` in the patched runtime path. Manual gameplay later showed this selector-side mitigation alone was not sufficient. |
| `xxd -p` checks at `0x429acf`/`0x495b00` for root `ManalinkEh.dll` and `0x3ec7cf`/`0x452d00` for `Program/ManalinkEh.dll`, plus restored Piranha/Bojuka callsites | Both DLLs hook `resolve_trigger()` before Spell Chain insertion for non-speculating AI-owned land `TRIGGER_COMES_INTO_PLAY` contexts, while the failed Piranha Marsh and Bojuka Bog wrapper callsites are restored to normal `comes_into_play()` calls. This is static/copied-install evidence; fresh manual Piranha/Bojuka proof is still required. |
| CrossOver `MTG` shortcut inspection | The visible shortcut targets `C:\Shandalar\Shandalar.exe`, not `C:\Shandalar\Program\Shandalar.exe`. |
| Earlier direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` with logging | Failed before gameplay because `Program\zlib.dll` was missing. This checkout and the local copied `MTG` install now include matching `Program/zlib.dll`; the copied `MTG` Program path still needs an exact-path visible retest after the later Program config/font and CardArt copies. |
| Bounded logged launch of `C:\Shandalar\Program\Shandalar.exe` in bottle `MTG` after the zlib layout fix | `/tmp/shandalar-mtg-program-zlib-retest-cx.log` shows native loads for `C:\Shandalar\Program\zlib.dll`, `image.dll`, `DrawCardLib.dll`, and `DECKDLL.dll`; a targeted fatal-error scan found no missing-library/import-failure/page-fault patterns. The run timed out as a GUI smoke and leftover processes were killed, so this is loader-path evidence only, not visible gameplay proof. |
| Visible direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the zlib layout fix | Reached a `drawcardlib.dll` missing-asset dialog for `C:\Shandalar\Program\CARDART\ManaSymbols.pic`. The repo and local copied install now include the Program CardArt, config, and font files listed in [runtime-manifest.md](runtime-manifest.md); exact-path visible retest is still pending. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the first Program CardArt copy | `/tmp/shandalar-mtg-program-cardart-retest-cx.log` loaded `ManaSymbols.pic`, `Expansion_Symbols.pic`, `Watermarks.pic`, and `CardCounters.png`, then reached a missing `C:\Shandalar\Program\CARDART\modern\Triggering.png` lookup. That file is now copied into the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the `Triggering.png` copy | `/tmp/shandalar-mtg-program-cardart-triggering-retest-cx.log` loaded `C:\Shandalar\Program\CARDART\modern\Triggering.png`, then reached a missing `C:\Shandalar\Program\CARDART\planeswalker\LoyaltyBase.png` lookup. The four Planeswalker loyalty images are now copied into the repo and local copied install; no further visible Program-path retest has been run. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the loyalty-image copy | `/tmp/shandalar-mtg-program-cardart-loyalty-retest-cx.log` loaded the loyalty-image fixes, then reached a missing `C:\Shandalar\Program\CARDART\modern\CardOv_Nyx.png` lookup. The generic Modern Nyx overlay is now copied into the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the `CardOv_Nyx.png` copy | `/tmp/shandalar-mtg-program-cardart-nyx-retest-cx.log` opened `ManaSymbols.pic`, `Modern/CardOv_Nyx.png`, and the other known Program CardArt files, then repeatedly looked for missing `C:\Shandalar\Program\Manalink.ini`. `Program/Manalink.ini` is now copied into the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the `Program/Manalink.ini` copy | `/tmp/shandalar-mtg-program-manalinkini-retest-cx.log` opened `C:\Shandalar\Program\Manalink.ini`, `Modern/CardOv_Nyx.png`, and the other known Program CardArt files, then repeatedly looked for missing `C:\Shandalar\Program\DuelArt\Modern.dat`. `Program/DuelArt/Modern.dat` is now copied into the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the `Modern.dat` copy | `/tmp/shandalar-mtg-program-moderndat-retest-cx.log` opened `Modern.dat`, `Program\Manalink.ini`, and known Program CardArt files, then repeatedly looked for missing `C:\Shandalar\Program\DuelArt\Planeswalker.dat` and Program `TT*.ttf` font files. Those files are now copied into the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the `Planeswalker.dat` and font copies | `/tmp/shandalar-mtg-program-planeswalker-font-retest-cx.log` opened `Program\DuelArt\Modern.dat`, `Program\DuelArt\Planeswalker.dat`, six Program `TT*.ttf` font paths, and then `C:\Shandalar\Program\Magic.exe`; the run exited with code 1. Targeted log scan found no new `drawcardlib.dll` missing-asset dialog, page fault, fatal assertion, or new Program data/art/font missing path. |
| Visible Program-path fatal dialog after the previous Program adjacent-data copies | The dialog reported `Bad raw_cards_data[-1].card() building Hornet: expected 15835, got -1`. Root `Cards.dat` contains a 16818-card database, while the pre-fix `Program/Cards.dat` header had 15718 cards; `Program/Cards.dat` and `Program/Rarity.dat` are now copied from root in the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the Program card-data copy | `/tmp/shandalar-mtg-program-carddata-retest-cx.log` opened `C:\Shandalar\Program\Cards.dat` with `ret 0`. Targeted log scan found no `raw_cards_data`, Hornet fatal, missing-asset dialog, page fault, or unhandled-exception strings. The process stayed alive until the 20-second alarm and was killed, so this is specific-fatal evidence only, not visible gameplay proof. |
| Visible Program-path fatal dialog after the `Cards.dat`/`Rarity.dat` copy | The dialog reported `Fatal: Couldn't find Cards.dat, DBInfo.dat or Rarity.dat`. `DeckDLL` source returns this same fatal when `DBInfo.dat` opens but its header count does not match `Cards.dat`; root `DBInfo.dat` has 16818 records while pre-fix `Program/DBInfo.dat` had 15718. `Program/DBInfo.dat` is now copied from root in the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the Program `DBInfo.dat` copy | `/tmp/shandalar-mtg-program-dbinfo-retest-cx.log` opened `C:\Shandalar\Program\DBInfo.dat` with `ret 0`. Targeted log scan found no generic card-data fatal, `raw_cards_data`, Hornet fatal, missing-asset dialog, page fault, or unhandled-exception strings. The process stayed alive until the 20-second alarm and was killed; cleanup tail includes `wineserver crashed`, so this is specific-fatal evidence only, not visible gameplay proof. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after the full Program card-data trio was present | `/tmp/shandalar-mtg-program-post-dbinfo-visible-retest-cx.log` opened `C:\Shandalar\Program\Cards.dat`, `DBInfo.dat`, `Rarity.dat`, `shandalar.dll`, and `Shandalar.ini`; the targeted scan found no generic card-data fatal, `raw_cards_data`, Hornet fatal, missing-asset dialog, page fault, assertion, or unhandled-exception strings. The process stayed alive past the 20-second alarm and was killed manually, so this is loader/fatal-regression evidence only. A later visible Hornet recurrence showed that the Program helper DLL generation also needed to match root. |
| Visible Program-path fatal dialog after the Program card-data trio was present | The dialog again reported `Bad raw_cards_data[-1].card() building Hornet: expected 15835, got -1`. The exact fatal string was present in the stale `Program/Shandalar.dll` generation, while the current root/Program synced helper DLLs no longer contain that assert string. `Program/Shandalar.dll`, `Program/CardArtLib.dll`, `Program/Deckdll.dll`, and `Program/Drawcardlib.dll` now match root in the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after syncing Program helper DLLs | `/tmp/shandalar-mtg-program-dll-sync-retest-cx.log` no longer showed the Hornet fatal, but the newer Program `DrawCardLib.dll` could not load because `C:\Shandalar\Program\libgcc_s_dw2-1.dll` was missing. `Program/libgcc_s_dw2-1.dll` now matches root in the repo and local copied install. |
| Bounded logged direct `MTG` launch of `C:\Shandalar\Program\Shandalar.exe` after adding `Program/libgcc_s_dw2-1.dll` | `/tmp/shandalar-mtg-program-dll-sync-libgcc-retest-cx.log` loaded `C:\Shandalar\Program\DrawCardLib.dll`, `libgcc_s_dw2-1.dll`, `DECKDLL.dll`, `shandalar.dll`, and the Program card-data files. The targeted scan found no generic card-data fatal, `raw_cards_data`, Hornet fatal, missing-asset dialog, missing-library import, page fault, assertion, or unhandled-exception strings. The process stayed alive until the 20-second alarm; leftover `Shandalar.exe` was killed manually, so this is loader/fatal-regression evidence only. |
| `rg -n "^Window\\s*=" Shandalar.ini Program/Shandalar.ini` | Both repo configs are set to `Window = 2` for the next start-color test. |
| `rg -n "^Window\\s*=" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/drive_c/Shandalar/Shandalar.ini" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/drive_c/Shandalar/Program/Shandalar.ini"` | The copied install inside bottle `MTG` is also set to `Window = 2`. |
| `rg -n "PagingFiles" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/system.reg"` | The local `MTG` bottle was updated from `C:\pagefile.sys 27 77` to `C:\pagefile.sys 512 1024`. |
| `sed -n '260,280p;790,825p;965,970p' "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/user.reg"` | Bottle `MTG` currently sets `DuelOptions` `ShowCoinFlips=0`, and sets `Shandalar.exe`, `Magic.exe`, and `FaceMaker.exe` to app-default `Version=win7` and desktop `Shandalar1440`; `Shandalar1440` is `1440x1080`. |
| Timed launch of `C:\Shandalar\Shandalar.exe` without explicit `--desktop` | Stayed alive until the alarm; log showed `wined3d`, `explorer.exe /desktop`, and 1024-wide 8bpp DIB sections. |
| Direct launch of patched `C:\Shandalar\FaceMaker.exe` | Rendered the FaceMaker UI in bottle `MTG`; `/tmp/facemaker-direct-after-patch-cx.log` showed no unhandled exception, page fault, or `WM_CREATE CreateDIBSection` assertion. |
| Visual launch of `C:\Shandalar\Shandalar.exe` | Reached the `Magic: Shandalar` main menu. |
| Visible `MTG` default/first start-color run via Wine SendKeys | Reached the adventure map without a visible `WM_CREATE CreateDIBSection` assertion; S2 is recorded in [manual-gameplay-verification.md](manual-gameplay-verification.md), with cropped evidence at [generated/manual-gameplay/s2-map-2026-05-31.png](generated/manual-gameplay/s2-map-2026-05-31.png) and notes in [generated/manual-gameplay/s2-map-2026-05-31.md](generated/manual-gameplay/s2-map-2026-05-31.md). The raw local log `/tmp/shandalar-visible-s2-attempt-cx.log` also observed `FaceMaker-nores.exe /S`. |
| Created `Shandalar-Win8-Test` CrossOver bottle | Real 32-bit `win8` template, `WineArch=win32`, Windows registry reports `CurrentVersion=6.2`, `CurrentBuild=9200`, `ProductName=Microsoft Windows 8`. |
| Timed launch of `Y:\Shandalar\Shandalar\Shandalar.exe` in `Shandalar-Win8-Test` | Stayed alive until killed after the timed smoke; log showed `wined3d` and many successful `NtGdiCreateDIBSection` calls. |
| Copied checkout into `Shandalar-Win8-Test` | Bottle-local install exists at `/Users/mdmoll/Library/Application Support/CrossOver/Bottles/Shandalar-Win8-Test/drive_c/Shandalar`, exposed to Wine as `C:\Shandalar`. |
| Configured `Shandalar-Win8-Test` taller virtual desktop | `FaceMaker.exe`, `Magic.exe`, and `Shandalar.exe` use desktop `ShandalarTall`, size `1024x800`. This is based on logged 1024x800-ish DIB creation, not yet a proven start-color fix. |
| Timed launch of `C:\Shandalar\Shandalar.exe` in `Shandalar-Win8-Test` | Stayed alive until cleanup after the timed smoke; log showed the C-drive install path and successful DIB creation. |
| Binary patches applied to root and `Program/` `Shandalar.exe` | File offset `0x1785b0` passes `hSection = NULL` to `CreateDIBSection`; `0xa1a42` seeds `mPlayer`; `0xa1acd` bypasses the fragile name editor; `0xa1af2` jumps to a fallback guard at `0x465170`/file offset `0x64570`; movement hooks at `0x44398c`, `0x444a2b`, and `0x444aa7` use code cave `0x46502d` and flag `0x583a2c` for same-arrow stop; file offset `0xcda2e` is `90 90 90 90 90`, NOPing the shell/window private MagSnd `UpdateSnd` message call; file offset `0xcdccd` begins `ff 05 f0 9d 58 00 c2 14 00`, reducing the WinMM timer callback to the required tick increment and five-argument callback return; file offset `0xcdd3f` remains `90 90 90 90 90`, NOPing the earlier WinMM callback-thread call to `0x56d476`; file offset `0x16c320` is `b8 04 00 00 00 c3`, returning sound-unavailable before the adventure executable loads `MagSnd.dll`; file offset `0x178010` is `31 c0 c3 90 90 90`, returning `NULL` before the imported MSVFW32 `MCIWndCreateA` path. Both files hash to `ebba01ad04aba5fb78841f37b6c264dfd17f1d6ca6ccfcc9851c2972b64f5f6b`. |
| Binary patches applied to root and `Program/` `Statwin.dll` | File offset `0x2a10` is `b8 07 00 00 00 c3`, returning video-unavailable before `Statwin.dll` loads `magvid.dll`; both files hash to `f1428cf548810f85df6f26b913d10dca16bc0f06a609a94c0cb0f0308347b0cf`. The matching local `MTG` copies preserve `*.before-magvid-loader-disable-patch.dll` backups. |
| Bounded logged direct launch of `C:\Shandalar\Shandalar.exe` in bottle `MTG` after the Drawcardlib linker fix | `/tmp/shandalar-regression-root-launch-post-drawcardlib-linkfix-cx.log` shows `DrawCardLib.dll` process attach reaching `START` and `END`; targeted scans found no `failed to initialize`, `Initialization of`, `EXCEPTION_ACCESS_VIOLATION`, page-fault, card-data fatal, or missing-asset strings. The process stayed alive until the 20-second alarm, and the child process left by the alarm was killed manually. This is loader-regression evidence only, not visible gameplay proof. |
| Local CrossOver bottle copies patched | `MTG` root and Program `C:\Shandalar` copies now hash to `ebba01ad04aba5fb78841f37b6c264dfd17f1d6ca6ccfcc9851c2972b64f5f6b`; `*.before-hsection-null-patch.exe` backups preserve the original bytes before the DIB patch, `*.before-winmm-tick-callback-patch.exe` backups preserve the bytes before the first WinMM callback patch, `*.before-magsnd-update-patch.exe` backups preserve the bytes before the MagSnd update-message patch, `*.before-magsnd-init-disable-patch.exe` backups preserve the bytes before disabling adventure-mode MagSnd initialization, `*.before-minimal-winmm-timer-callback-patch.exe` backups preserve the bytes before reducing the WinMM callback body, and `*.before-mciwndcreate-disable-patch.exe` backups preserve the bytes before disabling Shandalar's direct MCI window/video thunk. The older `Shandalar-Win8-Test` comparison bottle was not present during the 2026-06-07 MagSnd verification. |
| Patched `MTG` and `Shandalar-Win8-Test` `C:\Shandalar` launches | Wine `wscript` SendKeys drove the practical C-drive launch paths past the reported crash point; logs show `advfac64.pic`, `magic3.map`, `magic4.map`, and `begin.spr` loaded with no original page fault/assertion. |
| Same-arrow movement-stop patch | Static disassembly verifies hooks at `0x44398c`, `0x444a2b`, and `0x444aa7`, with code cave `0x46502d`; pressing the same movement key after at least one completed movement step should route through the existing Escape stop path at `0x444a96`. |
| `/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --version` | CrossOver 26.1.0 is installed locally. |
| `tools/verify-crossover-mtg-state.sh` | Optional local verifier passes for bottle `MTG`: patched root/Program `Magic.exe`, Shandalar MagSnd bytes, Statwin MagVid loader-disable bytes, Drawcardlib `DllCharacteristics=0x0000`, Manalink damage-prevention, AI decision-time clamp, AI raw-mana snapshot, Piranha Marsh trigger-target bytes, Bojuka Bog trigger-target bytes, Manalink and Shandalar generic AI player-target selector bytes, AI land CIP resolver stack-bypass bytes, root/Program `zlib.dll`, helper DLLs, card-data trio, Program adjacent config/font files, Program CardArt assets through `Modern/CardOv_Nyx.png`, FaceMaker support files, `AiDecisionTime=270`, `Window = 2`, `ShowCoinFlips=0`, app-default `win7`, `Shandalar1440=1440x1080`, and paging-file registry state. This is static copied-install evidence, not visible gameplay proof. |
| `command -v wine` | No standalone `wine` was found in `PATH`. |
| `xprotect version` | macOS XProtect reports `Version: 5346 Installed: 2026-05-28 08:53:39 +0000`. This is metadata only, not a repo file scan. |
| `xprotect status` | `XProtect launch scans: disabled`; `XProtect background scans: disabled`. No XProtect result is available for the required security-scan gate. |
| `clamscan --fail-if-cvd-older-than=7 --file-list=/private/tmp/shandalar-security-scan-target-paths-s2.txt --log=/private/tmp/shandalar-clamscan-s2.log` | ClamAV `1.5.2/28017/Sun May 31 02:27:13 2026` scanned 241 tracked security targets after the S2 evidence update and reported `Infected files: 0`; `tools/verify-security-scan-results.sh --results security-scan-results.tsv --require-all` validated 241 local TSV rows. |
| Earlier timed CrossOver launch attempts from `Program/` in bottle `MTG` | `Shandalar.exe --help`, `Shandalar.exe`, and `Magic.exe` exited with code 53 and no capturable stdout/stderr. This is not a gameplay verification. |
| `make -n` in `src/` | After restoring `src/card_id.h` from `Program/src/card_id.h`, dry run reaches the full `ManalinkEh.dll` compile/link plan. A real build remains unproven because required Windows-oriented tools are missing. |

## Automated Subset

Run the automated subset of these checks with:

```sh
tools/verify-share-readiness.sh
```

That script checks the clean tree, absence of tracked ignored files, Git binary
attributes, tracked fresh-install runtime/support files, patched runtime hashes,
install-tree hashes and patch bytes, representative patch bytes, core docs, and
local Markdown links. It does not launch the game.
