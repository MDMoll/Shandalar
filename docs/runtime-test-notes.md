# Runtime Test Notes

Use this file for short, chronological notes from bounded runtime attempts.
Long launch inventories belong in [running.md](running.md), full human gameplay
results belong in [manual-gameplay-verification.md](manual-gameplay-verification.md),
and raw/generated evidence belongs under [generated/](generated/).

Follow [runtime-testing-policy.md](runtime-testing-policy.md): one bounded
attempt per relevant target unless the user explicitly asks for more, then stop
and return to repository work if GUI automation is unreliable.

## Current Notes

| Date | Runtime | Result |
| --- | --- | --- |
| 2026-05-31 | CrossOver `MTG` | Root `C:\Shandalar\Shandalar.exe` reached the main menu and one default/first start-color path reached the adventure map. This is recorded as S1/S2 in [manual-gameplay-verification.md](manual-gameplay-verification.md). Remaining gameplay rows still need human-visible testing. |
| 2026-05-31 | CrossOver `MTG` | A later exploratory focus/SendKeys attempt became nondeterministic and was stopped. Per policy, do not keep iterating on GUI focus; use manual checklist rows or targeted one-shot logs instead. |
| 2026-06-03 | CrossOver `MTG` | Bounded logged direct Program-path run of `C:\Shandalar\Program\Shandalar.exe` from `C:\Shandalar\Program` loaded the earlier Program CardArt fixes through `Modern/Triggering.png`, then reached missing `C:\Shandalar\Program\CARDART\planeswalker\LoyaltyBase.png` in `/tmp/shandalar-mtg-program-cardart-triggering-retest-cx.log`. The four Planeswalker loyalty images are now copied into the repo and local `MTG` copied install; no further visible Program-path retest has been run. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run of `C:\Shandalar\Program\Shandalar.exe` from `C:\Shandalar\Program` loaded the loyalty-image fixes, then reached missing `C:\Shandalar\Program\CARDART\modern\CardOv_Nyx.png` in `/tmp/shandalar-mtg-program-cardart-loyalty-retest-cx.log`. `Program/DuelArt/Duel.dat` maps the Modern `CardOv_*Nyx` entries to this generic file; `Modern/CardOv_Nyx.png` is now copied into the repo and local `MTG` copied install. No further visible Program-path retest has been run. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the `CardOv_Nyx.png` copy opened the previously missing CardArt assets and then repeatedly looked for missing `C:\Shandalar\Program\Manalink.ini` in `/tmp/shandalar-mtg-program-cardart-nyx-retest-cx.log`. `Program/Manalink.ini` is now copied from active root `Manalink.ini` into the repo and local `MTG` copied install. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the `Program/Manalink.ini` copy opened `C:\Shandalar\Program\Manalink.ini`, the Program CardArt assets, and `Modern/CardOv_Nyx.png`, then repeatedly looked for missing `C:\Shandalar\Program\DuelArt\Modern.dat` in `/tmp/shandalar-mtg-program-manalinkini-retest-cx.log`. `Program/DuelArt/Modern.dat` is now copied from the preserved Program-style `Mods/Art/_undo/.../DuelArt/Modern.dat` snapshot into the repo and local `MTG` copied install. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the `Program/DuelArt/Modern.dat` copy opened `Modern.dat`, `Program\Manalink.ini`, and known Program CardArt assets, then repeatedly looked for missing `C:\Shandalar\Program\DuelArt\Planeswalker.dat` and the six Program `TT*.ttf` font files in `/tmp/shandalar-mtg-program-moderndat-retest-cx.log`. `Program/DuelArt/Planeswalker.dat` and the six Program font copies are now present in the repo and local `MTG` copied install. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the `Planeswalker.dat` and Program font copies opened `Program\DuelArt\Modern.dat`, `Program\DuelArt\Planeswalker.dat`, the six Program `TT*.ttf` font paths, and then `C:\Shandalar\Program\Magic.exe` in `/tmp/shandalar-mtg-program-planeswalker-font-retest-cx.log`; the run exited with code 1, with no new `drawcardlib.dll` missing-asset dialog, page fault, fatal assertion, or new Program data/art/font missing path found in the targeted log scan. This is loader/config evidence only, not visible gameplay proof. |
| 2026-06-04 | CrossOver `MTG` | User-visible Program-path fatal dialog reported `Bad raw_cards_data[-1].card() building Hornet: expected 15835, got -1`. Header/hash inspection showed `Program/Cards.dat` and `Program/Rarity.dat` were older 15718-record files, while root `Cards.dat` and `Rarity.dat` are matching 16818-record files. `Program/Cards.dat` and `Program/Rarity.dat` were copied from root in the repo and local `MTG` copied install. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the `Cards.dat`/`Rarity.dat` copy used `/tmp/shandalar-mtg-program-carddata-retest-cx.log`. The log opened `C:\Shandalar\Program\Cards.dat` with `ret 0`, and a targeted scan found no `raw_cards_data`, Hornet fatal, missing-asset dialog, page fault, or unhandled-exception strings. The process stayed alive until the 20-second alarm and was killed, so this is specific-fatal evidence only, not visible gameplay proof. |
| 2026-06-04 | CrossOver `MTG` | User-visible Program-path fatal dialog then reported `Fatal: Couldn't find Cards.dat, DBInfo.dat or Rarity.dat`. `DeckDLL` source returns that same fatal when `DBInfo.dat` opens but its record count does not match `Cards.dat`; header inspection showed root `DBInfo.dat` has 16818 records and Program `DBInfo.dat` still had 15718. `Program/DBInfo.dat` now matches root in the repo and local `MTG` copied install; visible Program-path retest is still pending. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the `DBInfo.dat` copy used `/tmp/shandalar-mtg-program-dbinfo-retest-cx.log`. The log opened `C:\Shandalar\Program\DBInfo.dat` with `ret 0`, and a targeted scan found no generic card-data fatal, `raw_cards_data`, Hornet fatal, missing-asset dialog, page fault, or unhandled-exception strings. The process stayed alive until the 20-second alarm and was killed; the cleanup tail includes a `wineserver crashed` line, so this is specific-fatal evidence only, not visible gameplay proof. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after the full Program card-data trio was present used `/tmp/shandalar-mtg-program-post-dbinfo-visible-retest-cx.log`. It opened `C:\Shandalar\Program\Cards.dat`, `DBInfo.dat`, `Rarity.dat`, `shandalar.dll`, and `Shandalar.ini` with `ret 0` or successful reads; a targeted scan found no generic card-data fatal, `raw_cards_data`, Hornet fatal, missing-asset dialog, page fault, assertion, or unhandled-exception strings. The child process stayed alive past the 20-second alarm and was killed manually, so this is stronger loader/fatal-regression evidence, not visible gameplay proof. |
| 2026-06-04 | CrossOver `MTG` | User-visible Program-path fatal dialog later reported the same Hornet `raw_cards_data[-1]` fatal. Strings and hash inspection showed the exact fatal string lived in the stale `Program/Shandalar.dll` helper generation, while root `Shandalar.dll` no longer had that assert string. `Program/Shandalar.dll`, `Program/CardArtLib.dll`, `Program/Deckdll.dll`, and `Program/Drawcardlib.dll` were synced from root in the repo and local `MTG` copied install, with bottle-local backups preserved. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after syncing the Program helper DLLs used `/tmp/shandalar-mtg-program-dll-sync-retest-cx.log`. It no longer showed the Hornet fatal, but failed to load the newer `DrawCardLib.dll` because adjacent `C:\Shandalar\Program\libgcc_s_dw2-1.dll` was missing. `Program/libgcc_s_dw2-1.dll` was copied from root into the repo and local `MTG` copied install. |
| 2026-06-04 | CrossOver `MTG` | Bounded logged direct Program-path run after adding `Program/libgcc_s_dw2-1.dll` used `/tmp/shandalar-mtg-program-dll-sync-libgcc-retest-cx.log`. It loaded `DrawCardLib.dll`, `libgcc_s_dw2-1.dll`, `DECKDLL.dll`, `shandalar.dll`, and the Program card-data files; targeted scan found no generic card-data fatal, `raw_cards_data`, Hornet fatal, missing-asset dialog, missing-library import, page fault, assertion, or unhandled-exception strings. The process stayed alive until the 20-second alarm; leftover `Shandalar.exe` was killed manually, so this is loader/fatal-regression evidence only. |
| 2026-06-04 | CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the local `MTG` copied install were patched in place so `_check_timer_for_ai_speculation` falls back to `270` instead of `5405` if `AiDecisionTime` is missing or invalid. Backups were preserved as `*.before-ai-decision-fallback-patch.dll` and `*.before-ai-decision-270-patch.dll`. This was later superseded by the AI clamp patch below. |
| 2026-06-05 | CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched so `_check_timer_for_ai_speculation` preserves configured values `1..270` and uses `270` for missing, invalid, or higher values. Backups were preserved as `*.before-ai-decision-clamp-patch.dll`, and `tools/verify-crossover-mtg-state.sh` verifies the current hashes and hook/cave bytes. This is static/copied-install evidence only, not gameplay proof. |
| 2026-06-05 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched so `_ai_decision_phase` saves both players' `raw_mana_available` rows before temporarily replacing only the opponent row during AI speculation. Backups were preserved as `*.before-ai-raw-mana-snapshot-patch.dll`, and `tools/verify-crossover-mtg-state.sh` verifies the current copied-install hashes and hook/cave bytes. This is static/copied-install evidence only, not gameplay proof. |
| 2026-06-05 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched so Piranha Marsh's ETB trigger uses `pick_player_duh()` instead of opening a generic player-target prompt for AI-controlled triggers. Backups were preserved as `*.before-piranha-marsh-target-patch.dll`, and `tools/verify-crossover-mtg-state.sh` verifies the copied-install hashes and bytes. This is static/copied-install evidence only, not a visible Piranha Marsh gameplay proof. |
| 2026-06-05 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched so Bojuka Bog's ETB trigger also uses `pick_player_duh()` for the same AI-controlled player-target trigger class. Backups were preserved as `*.before-bojuka-bog-target-patch.dll`, and `tools/verify-crossover-mtg-state.sh` verifies the copied-install hashes and bytes. This is static/copied-install evidence only, not a visible Bojuka Bog gameplay proof. |
| 2026-06-06 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched so `select_target_impl()` skips the generic selector for AI non-speculating pure `TARGET_ZONE_PLAYERS` targets after candidate construction has already filtered by `preferred_controller`. Backups were preserved as `*.before-ai-player-target-selection-patch.dll`, and `tools/verify-crossover-mtg-state.sh` verifies the copied-install hashes and hook/cave bytes. Manual Bojuka Bog gameplay testing later showed this selector-side mitigation was not sufficient. |
| 2026-06-06 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched so Piranha Marsh and Bojuka Bog preselect a valid AI player target during `EVENT_TRIGGER`, before the normal `comes_into_play()` path can expose the unresolved trigger through Spell Chain priority. Backups were preserved as `*.before-ai-etb-target-preselect-patch.dll`, but manual Bojuka Bog gameplay still froze afterward. This is failed mitigation evidence retained for history. |
| 2026-06-06 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched again so the exact non-speculating AI Piranha Marsh/Bojuka Bog `EVENT_TRIGGER` context resolves the small land ETB effect immediately and returns without placing the unresolved player-target trigger through Spell Chain. Backups were preserved as `*.before-ai-etb-target-immediate-patch.dll`, but manual Bojuka Bog gameplay still froze afterward. This is failed mitigation evidence retained for history. |
| 2026-06-06 | Repo and CrossOver `MTG` | Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched again so the exact non-speculating AI Piranha Marsh/Bojuka Bog context stored a valid player target and suppressed Spell Chain during `EVENT_TRIGGER`, then let the existing land ETB effect resolve during `EVENT_END_TRIGGER`. Backups were preserved as `*.before-ai-etb-target-end-trigger-patch.dll`, but manual Bojuka Bog gameplay later showed this end-trigger suppression candidate was still not sufficient. This is failed mitigation evidence retained for history. |
| 2026-06-06 | Repo and CrossOver `MTG` | Manual Bojuka Bog gameplay testing showed the end-trigger suppression candidate was still not sufficient. Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched again so the Piranha Marsh/Bojuka Bog `comes_into_play()` hook calls `comes_into_play_mode(..., RESOLVE_TRIGGER_AI(player))`, using the engine's normal AI trigger mode while the resolving block still uses `pick_player_duh()`. Backups were preserved as `*.before-ai-etb-trigger-mode-patch.dll`. This is static/copied-install evidence only; fresh manual Bojuka Bog proof is still required. |
| 2026-06-06 | Repo and CrossOver `MTG` | Manual Bojuka Bog gameplay testing showed the engine-native trigger-mode candidate was still not sufficient. Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched again so AI/Duh-owned Piranha Marsh and Bojuka Bog pass the normal ETB guard, apply their mandatory opponent-target effect during `EVENT_RESOLVE_TRIGGER`, write the opponent target, apply the effect directly, and return before the compiled picker path runs. Human-owned play keeps the normal trigger path. Backups were preserved as `*.before-ai-etb-resolve-trigger-guard-patch.dll`. This is static/copied-install evidence only; fresh manual Bojuka Bog proof is still required. |
| 2026-06-06 | Repo and CrossOver `MTG` | Manual Bojuka Bog gameplay testing showed the `duh_mode()`-guarded resolve-trigger candidate was still not sufficient. Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched again so raw AI-owned Piranha Marsh and Bojuka Bog use mandatory trigger discovery before consulting `duh_mode()`, and raw AI or Duh-mode `EVENT_RESOLVE_TRIGGER` handling returns before the compiled picker path. If the opponent target is invalid, the AI/Duh path now returns 0 instead of falling back to the picker. Backups were preserved as `*.before-ai-etb-ai-strict-resolve-patch.dll`. This is static/copied-install evidence only; fresh manual Bojuka Bog proof is still required. |
| 2026-06-06 | Repo and CrossOver `MTG` | Manual Piranha Marsh gameplay testing showed the strict raw-AI card-callsite wrapper was still not sufficient. Root and Program `ManalinkEh.dll` copies in the repo and local `MTG` copied install were patched again at the `resolve_trigger()` layer: the failed Piranha Marsh/Bojuka Bog wrapper callsites were restored to normal `comes_into_play()` calls, and AI-owned non-speculating land `TRIGGER_COMES_INTO_PLAY` triggers now resolve directly before `put_card_or_activation_onto_stack()` opens Spell Chain. Backups were preserved as `*.before-ai-land-cip-stack-bypass-patch.dll`. This is static/copied-install evidence only; fresh manual Piranha Marsh and Bojuka Bog proof is still required. |
| 2026-06-04 | CrossOver `MTG` | Root and Program `Magic.exe` copies in the repo and local `MTG` copied install were patched so missing `ShowCoinFlips` registry data defaults to `0` instead of `1`; active `MTG` `user.reg` was also changed from explicit `ShowCoinFlips=1` to `ShowCoinFlips=0`. Backups were preserved as `*.before-coinflip-default-patch.exe` and `user.before-coinflip-default-patch.reg`. This is static/copied-install evidence only, not a visible duel-start proof. |
| 2026-06-04 | CrossOver `MTG` | Bounded root direct-duel run used `C:\Shandalar\Shandalar.exe --e 0442 --p 0442` from `C:\Shandalar` with log `/tmp/shandalar-mtg-root-ep0442-coinflip-default-cx.log`. The process printed `Stand-alone duel: "decks/0442.dck" vs. "decks/0442.dck"`, opened root `C:\Shandalar\Magic.exe`, loaded AVI support, and opened `statwin\water.avi`. Targeted scans found no `COINTOSS`, `ShowCoinFlips`, `DIALOG_*COINFLIP`, Hornet/card-data fatal, page fault, DIB assertion, or unhandled-exception strings; only missing Wine audio-driver warnings matched the failure scan. The child process stayed alive until the 25-second alarm and was killed manually, so this is bounded crash-path evidence only, not visible gameplay proof. |
| 2026-06-05 | CrossOver `MTG` | After stopping an already-running Start Menu `Shandalar.lnk` wrapper, static verification showed the repo and copied `MTG` install have matching plain summoned-wizard deck pairs, and shortcut inspection showed `Shandalar.lnk` targets `C:\Shandalar\Shandalar.exe`. A bounded root direct-duel run used `C:\Shandalar\Shandalar.exe --e 0016 --p 0016` from `C:\Shandalar` with log `/tmp/shandalar-mtg-root-ep0016-dark-knight-cx.log`. It printed `Stand-alone duel: "decks/0016.dck" vs. "decks/0016.dck"`, opened root `Magic.exe`, and opened `C:\Shandalar\decks\0016.dck` after normal fallback misses for bare `0016` paths. Targeted scans found no Hornet/card-data fatal, page fault, DIB assertion, unhandled-exception, or coin-flip dialog strings. The child process stayed alive until killed manually; the cleanup tail includes a `wineserver crashed` line after the kill, so this is bounded handoff evidence only, not visible first-turn proof. |
| 2026-06-05 | CrossOver `MTG` | User reported root Shandalar no longer opened after the DeckDLL minimum-deck-size rebuild. Bounded launch command from repo root was `/usr/bin/perl -e 'alarm 15; exec @ARGV' /Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" --cx-log /tmp/shandalar-mtg-open-triage-cx.log --debugmsg +seh,+loaddll,+file "C:\Shandalar\Shandalar.exe"`; it exited in about 5 seconds with code 5. The log loaded `C:\Shandalar\DECKDLL.dll`, then crashed during `DECKDLL.dll` `PROCESS_ATTACH` at `addr=7A5B3102` while reading `CB210000`, followed by `DECKDLL.dll failed to initialize`. Static disassembly mapped this to MinGW startup image-base handling in the first rebuild. |
| 2026-06-05 | CrossOver `MTG` | Root and Program `DeckDLL.dll` copies in the repo and local `MTG` copied install were rebuilt and synced at SHA-256 `5c122ea5442d209d0d74c7e75f7b1f53492b0bfcc042efce49300f3485e3fcb0` with `___ImageBase=__image_base__`, `--disable-dynamicbase`, and `--disable-nxcompat`. Bounded launch command from repo root was `/usr/bin/perl -e 'alarm 15; exec @ARGV' /Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" --cx-log /tmp/shandalar-mtg-deckdll-nodynamicbase-fix-cx.log --debugmsg +seh,+loaddll,+file "C:\Shandalar\Shandalar.exe"`; the process stayed alive beyond the alarm and was killed manually. The log shows `MODULE_InitDLL (11000000 L"DECKDLL.dll",PROCESS_ATTACH,...) - CALL`, `MODULE_InitDLL (11000000,PROCESS_ATTACH,...) - RETURN 1`, and `process_attach (L"DECKDLL.dll",...) - END`, so the specific loader abort is repaired. |
