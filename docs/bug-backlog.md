# Bug Backlog

This backlog is evidence-based triage, not a claim that every item is a
runtime bug. Machine-readable source:
[generated/code-audit/potential-bugs.tsv](generated/code-audit/potential-bugs.tsv).

| Rank | ID | Title | Severity | Confidence | Area | Evidence | Proposed Fix | Test | Fix Risk |
| ---: | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | B002 | Windows-oriented build tools absent | high | high | build | no `yasm`, `dlltool`, `objcopy`, `windres` | Document exact toolchain; keep outputs separate | tool availability plus dry-run | safe |
| 2 | B003 | `src` and `Program/src` divergence is measured and partly guarded | high | high | source provenance | Parity report records 80 matching paths, 142 differing paths, and 100 paths missing from `Program/src`; exact-match files and current safety markers pass | Choose/record authoritative source snapshot; keep parity guard for mirrored safety edits | parity checker plus eventual build proof | safe |
| 3 | B006 | `CardArtLib` `Cards.dat` loader now fails closed on malformed input | low | medium | memory safety | `src/cardartlib/src/main.cpp` and `Program/src/cardartlib/src/main.cpp` check `fopen`, header reads, data reads, allocation, card ids, name offsets, and string termination | Add malformed `Cards.dat` harness when build/test support exists | static inspection, CardArtLib dry-runs, future malformed Cards.dat copy | safe |
| 4 | B012 | `Program/Shandalar.exe` adjacent runtime layout gaps are mitigated | low | high | runtime path | `Program/zlib.dll` now matches root `zlib.dll` in both this checkout and the local `MTG` copied install; bounded exact-path log loaded Program zlib/image/DrawCardLib/DECKDLL without the old fatal loader pattern; later `drawcardlib.dll` findings for missing `Program\CARDART\ManaSymbols.pic`, `Program\CARDART\modern\Triggering.png`, `Program\CARDART\planeswalker\LoyaltyBase.png`, and `Program\CARDART\modern\CardOv_Nyx.png` are addressed by Program CardArt asset copies; follow-up missing `Program\Manalink.ini`, Program `DuelArt` configs through `Planeswalker.dat`, and six hardcoded Program `TT*.ttf` font findings are addressed by adjacent Program config/font copies; later visible Program data fatals exposed older 15718-record `Cards.dat`/`Rarity.dat` and `DBInfo.dat` files, now replaced with matching root 16818-record copies | Keep root launch primary until exact Program path passes visibly | exact-path visible launch from `C:\Shandalar\Program` | safe |
| 5 | B010 | Draft custom-set parser now uses bounded line parsing | low | high | data parsing | `src/cards/draft.c` and `Program/src/cards/draft.c` use checked `fopen`, `fgets`, line stripping, slot/card-count guards, and no `%[` `fscanf` scans remain | Add malformed `DraftSets` harness when a build/test environment exists | static grep, source dry-runs, future malformed DraftSets copy | safe |
| 6 | B001 | Top-level `src` dry-run now reaches `ManalinkEh.dll` plan; real build remains blocked | medium | high | build | `src/card_id.h` restored from `Program/src/card_id.h`; `(cd src && make -n)` prints full compile/link plan; `tools/check-build-prereqs.sh --report-only` reports missing `yasm`, `dlltool`, `objcopy`, and `windres` | Install/document controlled Windows DLL toolchain and keep outputs isolated | `make -n`, preflight, then isolated real build only in a prepared toolchain | safe |
| 7 | B004 | `src/build.pl` mutates source when intentionally used, but external copies are now opt-in | low | high | tooling | script has `--dry-run`, `--out-dir`, `--copy-to`, and `--legacy-copy-targets`; no default `c:\magic2k` copy | Use `--dry-run` first; add deeper build-output isolation later if needed | `perl -c`; `--help`; dry-run from `src` | safe |
| 8 | B005 | `src/deploy.bat` destructive deploy now requires explicit confirmation | low | high | tooling | `src/deploy.bat` exits before mutation unless called with `--confirmed-c-magic2k-deploy`; confirmed mode still deletes/copies under `c:\magic2k` | Keep historical; run only in prepared Windows packaging copy | static review | safe |
| 9 | B013 | Root and `Program` `Magic.exe` differ but both matter | medium | high | runtime path | different hashes and separate launch paths | Always patch/test both exact paths | hash and launch matrix | safe |
| 10 | B014 | Root and `Program` `ManalinkEh.dll` differ by hash/offset | medium | high | patch risk | damage-prevention, AI clamp, AI raw-mana snapshot, Piranha Marsh trigger-target, and Bojuka Bog trigger-target offsets differ by path | Path-specific hash/offset verification | `xxd`/`shasum` | safe |
| 11 | B015 | Declared-attacker undo may not reverse costs/triggers | medium | medium | gameplay state | docs note attack hooks can run before selection completes | Keep manual-test gate; avoid more patching | gameplay R3/R4 and edge cases | do-not-change-yet |
| 12 | B016 | Damage-prevention prompts may still freeze without manual proof | medium | medium | gameplay state | source snapshots and runtime DLLs now guard generic activated `GAA_DAMAGE_PREVENTION*` paths; handler inventory records 127 rows covered by `granted_generic_activated_ability()` and 2 Samite-family rows with same-function guards | Keep runtime hash/offset verification and run visible manual repros before declaring gameplay fixed | source inventory plus runtime hash/offset proof and manual repro by card | needs manual test |
| 13 | B017 | Unimplemented DDBM/subtype flags assert | medium | high | crash risk | `ASSERT(... "unimplemented")` in source | Convert only with tests | targeted scenario tests | risky |
| 14 | B018 | GDI lifetime is hand-managed in many UI paths | medium | medium | resource lifetime | repeated `CreateCompatibleDC`, `SelectObject`, `DeleteObject` | Audit one module at a time | Windows GDI handle monitoring | moderate |
| 15 | B007 | `CardArtLib` art paths no longer use fixed path/name buffers | low | medium | memory safety | `idToNameFun()` builds `std::string` paths, validates id/name lookups, and image-counting uses string names instead of fixed `char tmp[254]` / `char cardname[200]` buffers | Add long-name art harness when build/test support exists | static inspection and CardArtLib dry-runs | safe |
| 16 | B008 | `drawcardlib` frame-name formatting now uses bounded diagnostics | low | medium | memory safety | `src/drawcardlib/config.c` and `Program/src/drawcardlib/config.c` use larger static diagnostic buffers plus `snprintf` in `get_pichandlename()` | Add long-path/config-name harness when build/test support exists | static grep, drawcardlib dry-runs, future long-path copy | safe |
| 17 | B009 | Config key concatenation now avoids fixed 104-byte buffers | low | medium | memory safety | `src/drawcardlib/config.c` and `Program/src/drawcardlib/config.c` use allocated INI keys for `get_cfg_int2_raw()` and font lookup | Add long-key config harness when build/test support exists | static grep, drawcardlib dry-runs, future long-key config copy | safe |
| 18 | B019 | Launcher mutates local runtime/mod trees | medium | high | runtime layout | `Manalink_Launcher.cmd` deletes temp, installs mods, deletes draft decks | Use disposable-copy tests for launcher work | before/after tree diff | safe |
| 19 | B011 | Draft mode output paths are still runtime-local, but output opens now fail closed | low | high | runtime layout | `src/cards/draft.c` and `Program/src/cards/draft.c` use `open_draft_output()` for `draft_errors.txt`, `picks.txt`, `packs.txt`, and deck writes; invalid AI picks are skipped before score-log dereference | Keep CWD behavior documented; consider configurable output directory only after draft-mode copy test | static grep, parity checker, future draft-mode copy test | safe for guard; moderate for relocation |
| 20 | B020 | Optional static analysis is missing | low | high | tooling | optional analyzers not installed | Add optional analyzer workflow later | analyzer reports | safe |
| 21 | B021 | Auto-target text parser now uses bounded line parsing | low | high | data parsing | `src/functions/targets.c` and `Program/src/functions/targets.c` use checked `fopen`, bounded `fgets`, numeric-line parsing, and reserve space for the `-1` sentinel | Add malformed `TargetsHuman.txt`/`TargetsAI.txt` harness when build/test support exists | static grep, source dry-runs, future malformed target-list copy | safe |
| 22 | B022 | Duel-start coin-flip animation can freeze before first turn | medium | medium | runtime/UI | user-visible freeze report before coin flip; `Magic.exe` contains `ShowCoinFlips`, `DIALOG_STARTCOINFLIP`, and `COINTOSS_*.AVI` paths; active `MTG` registry had explicit `ShowCoinFlips=1` before this pass | Default missing registry value to off and set active `MTG` `ShowCoinFlips=0`; keep visible retest gate | byte/hash checks, registry check, manual duel-start test | moderate |
| 23 | B023 | AI speculation raw-mana snapshot restored one player row from uninitialized stack data | medium | high | gameplay state | `src/functions/ai.c` and both shipped `ManalinkEh.dll` copies saved only `raw_mana_available[1 - player]` before restoring both player rows after speculation | Save both player rows before temporarily replacing only the opponent row; sync copied installs only with explicit approval | source diff, `xxd`/`objdump` cave checks, opponent-turn manual retest | moderate |
| 24 | B024 | Piranha Marsh AI trigger could reach a generic player-target prompt | medium | high | gameplay state | User reported opponent Piranha Marsh froze with the Spell Chain box movable; `card_piranha_marsh()` used `pick_target()` during its ETB trigger | Route the trigger through `pick_player_duh()` and patch both shipped `ManalinkEh.dll` copies plus copied installs | source diff, `xxd`/`objdump` site checks, Piranha Marsh manual retest | moderate |
| 25 | B025 | Bojuka Bog AI trigger used the same generic player-target path | medium | high | gameplay state | User reported Bojuka Bog as another land in the same Spell Chain freeze class; `card_bojuka_bog()` used `pick_target()` during its ETB trigger | Route the trigger through `pick_player_duh()` and patch both shipped `ManalinkEh.dll` copies plus copied installs | source diff, `xxd`/`objdump` site checks, Bojuka Bog manual retest | moderate |
| 26 | B026 | Mandatory AI player-only ETB triggers can hit the generic target selector | medium | high | gameplay state | Source scan found many mandatory ETB-style player-target triggers still using `pick_target()`/`TARGET_PLAYER`; `select_target_impl()` called `sub_499050()` even after AI candidate construction had already filtered player targets | Short-circuit only AI non-speculating pure `TARGET_ZONE_PLAYERS` targets after candidate construction; patch both shipped `ManalinkEh.dll` copies plus copied installs | source diff, hook/cave `xxd`/`objdump` checks, broad ETB manual retest | moderate |
