# AGENTS.md

This repo is a mixed game/runtime/assets/source/history checkout. Future agents
should keep the working tree safe and evidence-driven.

## Guardrails

| Rule | Why |
| --- | --- |
| Do not delete, move, or normalize binaries, art, data, decks, sounds, or historical files without explicit user approval. | Old game launch paths often depend on nearby files and exact names. |
| Treat `.exe`, `.dll`, `.dat`, `.pic`, `.spr`, `.csv`, `.dck`, `.cat`, `.res`, `.wav`, `.ogg`, `.avi`, and art/deck folders as potentially runtime-critical until proven otherwise. | Many of these are proprietary resource formats or runtime data. |
| Keep CrossOver/Wine notes separate from native Windows notes. | Bottle behavior is not the same as Windows behavior. |
| Preserve case and spacing in filenames. | Windows behavior, legacy scripts, and old resource lookups may rely on case-insensitive or short-name assumptions. |
| Do not assume `src/` rebuilds the shipped binaries. | Current evidence shows source and patch tooling, but not an end-to-end reproducible build. |
| Prefer docs and audit reports over cleanup. | The current repo needs mapping more than destructive pruning. |
| Record every launch/build command and its working directory. | Working directory is likely significant for both executables. |
| Do not download redistributable installers into the repo. | Runtime packages should be installed into Windows/CrossOver bottles by the user. |
| Do not claim the repo is safe for public redistribution unless [docs/distribution.md](docs/distribution.md) has been updated with evidence. | The checkout contains bundled game binaries, art, data, decks, and rightsholder/trademark notices, and no repository-level license file was found. |
| Do not declare binaries safe unless a named scanner was actually run and recorded. | Old game binaries can trigger false positives, but that is not proof of safety. |
| Put scan guidance/results in [docs/security-scan.md](docs/security-scan.md). | Safety claims need scanner name, version, path, hash, date, and result. |
| Follow [docs/runtime-testing-policy.md](docs/runtime-testing-policy.md) before launching Wine/CrossOver or adding GUI helpers. | Runtime testing is secondary evidence; if focus, SendKeys, or display mode becomes unreliable, stop and resume repo work. |
| Preserve the root `.gitattributes` binary rules and do not run broad `git add --renormalize` without a separate review. | Legacy resource formats, saves, media, archives, and executables should not be rewritten by text filters or line-ending cleanup. |
| Do not casually replace the patched `Shandalar.exe` binaries with older copies. | Root and `Program/Shandalar.exe` contain documented patches at `0x1785b0` for the start-color `CreateDIBSection` crash, `0xa1a42` for the name-entry default buffer, `0xa1acd`/`0xa1af2` for the name-editor bypass and empty-name fallback, movement hooks at `0x44398c`, `0x444a2b`, and `0x444aa7` using code cave `0x46502d`, a MagSnd update-message compatibility patch at VA `0x4ce62e` / file offset `0xcda2e` that NOPs the shell/window `UpdateSnd` message call, a minimal WinMM timer-callback patch at VA `0x4ce8cd` / file offset `0xcdccd` that preserves only `inc dword [0x589df0]; ret 0x14`, the earlier callback-thread `call 0x56d476` NOP at VA `0x4ce93f` / file offset `0xcdd3f`, a MagSnd initialization-disable compatibility patch at VA `0x56cf20` / file offset `0x16c320` that returns `b8 04 00 00 00 c3` before loading `MagSnd.dll`, and an MCIWndCreateA disable stub at VA `0x578c10` / file offset `0x178010` that returns `31 c0 c3 90 90 90` before entering Wine's MSVFW32 MCI window path. |
| Do not casually replace the active `FaceMaker.exe` binaries with the older no-resolution copies. | Root and `Program/FaceMaker.exe` are now no-resolution/Korath-derived plus an additional documented `CreateDIBSection` compatibility patch. |
| Do not archive or delete `FaceMaker-nores.exe` as stale clutter. | A visible 2026-05-31 CrossOver new-game run logged `FaceMaker-nores.exe /S` during the path that reached the adventure map, so the no-resolution helper is runtime-relevant evidence until the character-creation handoff is fully understood. |
| Do not casually replace the patched `Shandalar.dll` binaries with older copies. | Root and `Program/Shandalar.dll` are byte-for-byte matched at SHA-256 `f74648745315163da15ffbe32e5bbdbc79e05aaf47c0714902c8d6898e5d00f7` and contain an AI land CIP resolver stack-bypass hook at `0x94d34` into executable `.cdxai` cave `0x1174800`, plus an AI player-only target selector hook at `0xcb16` into `.cdxai` cave `0x1174920`; these target Shandalar adventure-duel freezes where `Shandalar.exe` loads `Shandalar.dll` rather than `ManalinkEh.dll`, including the broader Augur/Piranha/Bojuka class of AI-controlled player-target effects. |
| Do not casually replace the patched `Statwin.dll` binaries with older copies. | Root and `Program/Statwin.dll` are byte-for-byte matched at SHA-256 `f1428cf548810f85df6f26b913d10dca16bc0f06a609a94c0cb0f0308347b0cf` and contain a MagVid loader-disable compatibility patch at VA `0x10003610` / file offset `0x2a10`, returning `b8 07 00 00 00 c3` so the status/video helper reports video unavailable before loading `magvid.dll`; this targets the same `EIP == EDX == 0xfff50de4` Augur/announcement freeze class after `MagSnd.dll` was no longer loaded. |
| Do not casually replace the rebuilt `CardArtLib.dll` binaries with older copies. | Root and `Program/CardArtLib.dll` are byte-for-byte matched at SHA-256 `c1a68591059ff3e650104bf711d4e3f0c9a01a232db2e594af64aaa6846b3c1d` and were rebuilt from `src/cardartlib` so CardArtLib starts GDI+ lazily outside `DllMain`, uses explicit notification hook/unhook handling, suppresses external codecs, validates image load status before caching, removes the old Boost dependency, and keeps PE `DllCharacteristics` `0x0000` via `___ImageBase=__image_base__`, `--disable-dynamicbase`, and `--disable-nxcompat`; the first rebuild without `___ImageBase` failed during `Cardartlib.dll` process attach in CrossOver. |
| Do not casually replace the rebuilt `Drawcardlib.dll` binaries with older copies. | Root and `Program/Drawcardlib.dll` are byte-for-byte matched at SHA-256 `d09188b790ad8cb363b2428c8636b412caa6267c0d8e826abdb255072c3aef3f` and were rebuilt from `src/drawcardlib` so Drawcardlib suppresses GDI+ external codecs, uses explicit notification hook/unhook handling instead of allowing GDI+ to create an internal background thread, checks the notification hook status, and keeps PE `DllCharacteristics` `0x0000` via `___ImageBase=__image_base__`, `--disable-dynamicbase`, and `--disable-nxcompat` after the first GDI+ rebuild crashed during `DrawCardLib.dll` process attach in CrossOver. This targets Thorn Thallid and broader Spell Chain/card-rendering freezes that still hit `EIP == EDX == 0xfff50de4` after MagSnd and MagVid were absent while preserving legacy loader behavior. |
| Do not casually replace the patched `ManalinkEh.dll` binaries with older copies. | Root and `Program/ManalinkEh.dll` contain a documented Samite/Femeref/Kithkin damage-prevention activation guard at file offsets `0x3bb035` and `0x381a25`, a generic activated damage-prevention helper guard via jumps at `0x44cb23` and `0x40f115` into executable caves at `0x495a30` and `0x452c30`, an AI decision-time clamp via jumps at `0x40d0e1` and `0x3d2da1` into caves at `0x495a60` and `0x452c60`, an AI raw-mana speculation snapshot fix via jumps at `0x40db84` and `0x3d3844` into caves at `0x495a90` and `0x452c90`, Piranha Marsh and Bojuka Bog trigger-target patches at offsets `0x3fe7a0`/`0x3c4930` and `0x3f63e0`/`0x3bc630`, a generic AI player-only target-selection short-circuit via jumps at `0x469583` and `0x429453` into caves at `0x495ad0` and `0x452cd0`, and an AI land CIP resolver stack-bypass via jumps at `0x429acf` and `0x3ec7cf` into caves at `0x495b00` and `0x452d00` with the failed Piranha Marsh/Bojuka Bog callsite wrappers restored to normal `comes_into_play()` calls at `0x3fe77d`/`0x3f63bd` and `0x3c490d`/`0x3bc60d`; these patches target duel freezes where a damage-prevention ability was offered outside the real damage-prevention window, where missing, invalid, or too-high `AiDecisionTime` could let opponent speculation look frozen, where AI speculation restored one player's raw mana from uninitialized stack bytes, and where AI-controlled land ETB triggers could reach Spell Chain priority machinery before resolving. |
| Do not casually replace the patched `Magic.exe` binaries with older copies. | Root and `Program/Magic.exe` contain a documented declared-attacker undo hook at `0x43c303` with code cave `0x459bc8`, plus a `ShowCoinFlips` missing-registry default patch at file offset `0x5db1f`; these let a human player click an already-declared attacker before Done to clear `STATE_ATTACKING` and keep the duel-start coin-flip animation disabled by default. |
| Do not treat every `.tmp` file as disposable. | The tracked `Statwin/*.tmp` and `Program/statwin/*.tmp` files are bitmap/mask-like UI resources by `file` output; root and Program `statscrn.tmp` also differ by hash. |
| Treat duplicate cleanup as an install-root decision first. | The active canonical layout is repo root plus top-level `Program/` and top-level `Mods/`; `Manalink3/` is historical/unsupported for active cleanup unless a future user decision says otherwise. |

## Archive Policy

| Rule | Why |
| --- | --- |
| `archive/` is preserved evidence/history, not a deletion staging area. | Archived files may still explain old packaging, debugging, or local behavior. |
| It is acceptable to add clearly non-runtime evidence to `archive/` with documentation. | This keeps the root navigable without losing context. |
| Do not move runtime-like assets into `archive/` without explicit approval and a launch-copy test plan. | Many root files mirror or differ from `Program/` files in ways that are not fully understood. |
| Use [docs/cleanup-launch-copy-test.md](docs/cleanup-launch-copy-test.md) before proposing additional risky cleanup moves. | Candidate removals should be proven in a disposable copy before the real checkout changes. |
| When moving a tracked file, use `git mv` and update [docs/reorganization.md](docs/reorganization.md). | History and path evidence need to remain inspectable. |
| Do not restore duplicate archives under `Manalink3/Mods/` just to make that package root self-contained. | [docs/package-layout-cleanup.md](docs/package-layout-cleanup.md) documents top-level `Mods/` as the canonical archive root after the install-root policy pass. |

## Default Launch Context

| Target | Working directory | Notes |
| --- | --- | --- |
| Root `Shandalar.exe` | `/Users/mdmoll/Shandalar/Shandalar` or `C:\Shandalar` in copied CrossOver bottle `MTG` | Current repo copy is patched for the start-color `CreateDIBSection` crash, default-name handling with name-editor bypass/fallback, same-arrow map stop, the MagSnd update-message compatibility path, the minimal WinMM timer callback, the MagSnd init-disable compatibility path, and the MCIWndCreateA disable path, hashes to `ebba01ad04aba5fb78841f37b6c264dfd17f1d6ca6ccfcc9851c2972b64f5f6b`, and has the same SHA-256 as `Program/Shandalar.exe`; the local `MTG` copied install was patched too, with `Shandalar.before-hsection-null-patch.exe`, `Shandalar.before-winmm-tick-callback-patch.exe`, `Shandalar.before-magsnd-update-patch.exe`, `Shandalar.before-magsnd-init-disable-patch.exe`, `Shandalar.before-minimal-winmm-timer-callback-patch.exe`, and `Shandalar.before-mciwndcreate-disable-patch.exe` backups preserving earlier bytes. A 2026-06-08 priestess pre-card freeze still faulted with `EIP == EDX == 0xfff50de4` after the bottle matched all earlier patches and live inspection showed no `Magic.exe`, `MagSnd.dll`, `magvid.dll`, or `ManalinkEh.dll`; the MCI patch avoids Shandalar's remaining direct MSVFW32 MCI window/video path. Manual gameplay retesting remains required. |
| Root `Magic.exe` | `/Users/mdmoll/Shandalar/Shandalar` or `C:\Shandalar` in copied CrossOver bottle `MTG` | Logged root Shandalar startup opens this copy. It differs by SHA-256 from `Program/Magic.exe`, has the declared-attacker undo patch at `0x43c303`/`0x459bc8`, has the `ShowCoinFlips` missing-registry default-off patch at file offset `0x5db1f`, and currently hashes to `93a40ce2c96aafee1d858a71ed69eb8c539aa9851796eb54b1af58f0bb97aba0`; the local `MTG` copy was patched too, with `Magic.before-declared-attacker-undo-patch.exe` and `Magic.before-coinflip-default-patch.exe` preserving earlier bytes. The active `MTG` `user.reg` also sets `DuelOptions` `ShowCoinFlips` to `0`, because an explicit registry value overrides the binary default. Root `ManalinkEh.dll` is patched for the Samite-family and generic activated damage-prevention freeze paths, AI decision-time clamping, AI raw-mana speculation snapshot restore safety, Piranha Marsh/Bojuka Bog trigger targeting, generic AI player-only target selection, and AI land CIP resolver stack-bypass handling, and currently hashes to `68f2ba31f26f99edfb0944fe3fbc577ef0a42f9f6a6d7d44cb3aaa5f9b9cadd5`. |
| `Program/Shandalar.exe` | `/Users/mdmoll/Shandalar/Shandalar/Program` | Same patched SHA-256 as root `Shandalar.exe`; this checkout and the local `MTG` copied install now have adjacent `Program\zlib.dll` matching root `zlib.dll` at SHA-256 `9f8729ac49e0ccea86fe3b1a9b2c3fae9986ecd09db92853e7a588dbda85bf90`, `Program\Manalink.ini` matching root `Manalink.ini` at SHA-256 `30153fd22c76b0c0751c538938af46fbf25b1b51d5b4bb2bd9a2eead1b9c2f2b`, `Program\Cards.dat`, `Program\DBInfo.dat`, and `Program\Rarity.dat` matching the newer root card-data trio, matching root/Program helper DLLs (`Shandalar.dll`, rebuilt `CardArtLib.dll`, `Deckdll.dll`, and rebuilt `Drawcardlib.dll`), matching root/Program `libgcc_s_dw2-1.dll`, Program `DuelArt` frame configs through `Planeswalker.dat`, the six hardcoded Program `TT*.ttf` drawcard fonts, plus Program CardArt drawcardlib assets observed so far through `Modern/CardOv_Nyx.png`. Direct copied-bottle `C:\Shandalar\Program\Shandalar.exe` previously failed before the DLL was present, then reached `drawcardlib.dll` missing-asset findings, `Program\Manalink.ini`, `Program\DuelArt\Modern.dat`, `Program\DuelArt\Planeswalker.dat`, Program `TT*.ttf` lookups, a visible `raw_cards_data[-1]` Hornet fatal from older 15718-record Program data, DeckDLL's generic `Cards.dat, DBInfo.dat or Rarity.dat` fatal from still-older Program `DBInfo.dat`, and a later visible Hornet recurrence from the stale `Program\Shandalar.dll` helper generation. `/tmp/shandalar-mtg-program-dll-sync-libgcc-retest-cx.log` loaded the synced Program helper DLLs, `libgcc_s_dw2-1.dll`, and Program card-data files without the earlier fatal strings. Keep root `C:\Shandalar\Shandalar.exe` as the primary `MTG` path until exact Program-path visible retesting passes. |
| `Program/Magic.exe` | `/Users/mdmoll/Shandalar/Shandalar/Program` | Manalink launcher path. `Program/Magic.exe` and root `Magic.exe` differ by SHA-256, so test and document the path used. It has the same declared-attacker undo hook at `0x43c303`/`0x459bc8`, the same `ShowCoinFlips` default-off patch at file offset `0x5db1f`, and currently hashes to `685669692634ec830fe228904e11b1b536bd4b20e52192863a6280c2dbff6b66`; the local `MTG` copy was patched too, with `Program/Magic.before-declared-attacker-undo-patch.exe` and `Program/Magic.before-coinflip-default-patch.exe` preserving earlier bytes. `Program/ManalinkEh.dll` has the same damage-prevention, AI clamp, AI raw-mana speculation snapshot, Piranha Marsh/Bojuka Bog trigger-target, generic AI player-only target-selection, and AI land CIP resolver stack-bypass patches at different offsets and currently hashes to `619ce5d3f80f4ac951418e8a1b2ec803b3b9aa0128e01b827e744b80e63962fc`. |
| `FaceMaker.exe` / `FaceMaker-nores.exe` | Same folder as the active Shandalar launch | New-game character creation depends on the FaceMaker helper family plus `FaceData.txt`, `FaceButtons.txt`, and face art. Active root and `Program` `FaceMaker.exe` copies are patched from the no-resolution/Korath helper and no longer match `FaceMaker-nores.exe`; however, the 2026-05-31 visible S2 run logged `FaceMaker-nores.exe /S`, so preserve both names. |
| `Manalink_Launcher.cmd` | repo root on Windows | The batch file sets `mlDir` to `Program` and starts `Magic.exe`. |

## Useful Inspection Commands

Run from `/Users/mdmoll/Shandalar/Shandalar` unless noted.

| Task | Command |
| --- | --- |
| List tracked docs | `git ls-files | rg -i '^(readme|agents|docs/)'` |
| Check PE architecture | `file Program/Shandalar.exe Program/Magic.exe Program/*.dll` |
| Inspect imports | `objdump -p Program/Magic.exe | rg "DLL Name:"` |
| Hash launch targets before scan reports | `shasum -a 256 Program/Shandalar.exe Program/Magic.exe Shandalar.exe Magic.exe` |
| Verify the Shandalar DIB patch | Run `xxd -g1 -l 32 -s $((0x1785b0)) Shandalar.exe` and `xxd -g1 -l 32 -s $((0x1785b0)) Program/Shandalar.exe`; expect `6a 00 57 50 8b 4d 10 51 ff 75 04` at the start of each dump. |
| Verify the Shandalar name-entry patch | Run `xxd -g1 -l 40 -s $((0xa1a42)) Shandalar.exe` and `xxd -g1 -l 40 -s $((0xa1a42)) Program/Shandalar.exe`; expect bytes that write `mPlayer` into `0x591228`, starting `c7 05 28 12 59 00 6d 50 6c 61`. |
| Verify the Shandalar name-editor bypass | Run `xxd -g1 -l 32 -s $((0xa1acd)) Shandalar.exe` and `xxd -g1 -l 32 -s $((0xa1acd)) Program/Shandalar.exe`; expect `31 c0 89 85 a8 fe ff ff` followed by NOPs, which skips the fragile manual name editor and treats the default name as accepted. |
| Verify the Shandalar empty-name fallback | Run `xxd -g1 -l 64 -s $((0x64570)) Shandalar.exe` and `xxd -g1 -l 64 -s $((0x64570)) Program/Shandalar.exe`; expect code that compares byte `[0x591228]` with zero and writes `Player` before copying to `0x7a0770` if needed. |
| Verify the Shandalar movement-stop patch | On this Mac, use `lldb -b -o 'target create Shandalar.exe' -o 'disassemble --start-address 0x44398c --end-address 0x4439a0' -o 'disassemble --start-address 0x444a2b --end-address 0x444a38' -o 'disassemble --start-address 0x444aa7 --end-address 0x444ab2' -o 'disassemble --start-address 0x46502d --end-address 0x4651a4' -o quit`; expect jumps into code cave `0x46502d`, flag storage at `0x583a2c`, and same-key stop branches to `0x444a96`. GNU `objdump -D -Mintel` is fine when available. |
| Verify the Shandalar MagSnd update-message patch | Run `xxd -p -l 5 -s $((0xcda2e)) Shandalar.exe` and `xxd -p -l 5 -s $((0xcda2e)) Program/Shandalar.exe`; expect `9090909090`, which NOPs the shell/window private `0x10101010` MagSnd `UpdateSnd` message call. |
| Verify the Shandalar minimal WinMM timer-callback patch | Run `xxd -p -l 16 -s $((0xcdccd)) Shandalar.exe` and `xxd -p -l 16 -s $((0xcdccd)) Program/Shandalar.exe`; expect `ff05f09d5800c2140090909090909090`, which increments the 33 ms tick counter and returns from the five-argument WinMM callback without thread bookkeeping. |
| Verify the Shandalar WinMM timer-callback patch | Run `xxd -p -l 5 -s $((0xcdd3f)) Shandalar.exe` and `xxd -p -l 5 -s $((0xcdd3f)) Program/Shandalar.exe`; expect `9090909090`, which NOPs the callback-thread `call 0x56d476` while preserving the 33 ms tick counter. |
| Verify the Shandalar MagSnd init-disable patch | Run `xxd -p -l 6 -s $((0x16c320)) Shandalar.exe` and `xxd -p -l 6 -s $((0x16c320)) Program/Shandalar.exe`; expect `b804000000c3`, which returns sound-unavailable before the adventure executable loads `MagSnd.dll`. |
| Verify the Shandalar MCIWndCreateA disable patch | Run `xxd -p -l 6 -s $((0x178010)) Shandalar.exe` and `xxd -p -l 6 -s $((0x178010)) Program/Shandalar.exe`; expect `31c0c3909090`, which returns NULL before entering the imported MSVFW32 `MCIWndCreateA` thunk. |
| Verify the active Shandalar hash | `shasum -a 256 Shandalar.exe Program/Shandalar.exe`; both should be `ebba01ad04aba5fb78841f37b6c264dfd17f1d6ca6ccfcc9851c2972b64f5f6b` until a later documented patch changes them. |
| Verify the Statwin MagVid loader-disable patch | Run `xxd -p -l 6 -s $((0x2a10)) Statwin.dll` and `xxd -p -l 6 -s $((0x2a10)) Program/Statwin.dll`; expect `b807000000c3`, which returns video-unavailable before `Statwin.dll` loads `magvid.dll`. |
| Verify the Program Shandalar helper DLL sync | `shasum -a 256 Shandalar.dll Program/Shandalar.dll Statwin.dll Program/Statwin.dll CardArtLib.dll Program/CardArtLib.dll DeckDLL.dll Program/Deckdll.dll Drawcardlib.dll Program/Drawcardlib.dll libgcc_s_dw2-1.dll Program/libgcc_s_dw2-1.dll`; root/Program pairs should match, with `Program/Deckdll.dll` preserving its existing filename case. Then run `tools/verify-install-tree.sh .` or `tools/verify-crossover-mtg-state.sh` to verify the CardArtLib and Drawcardlib PE `DllCharacteristics` remain `0x0000`. |
| Verify the Program adjacent config files | `shasum -a 256 Program/Manalink.ini Program/DuelArt/Modern.dat Program/DuelArt/Planeswalker.dat`; expect `30153fd22c76b0c0751c538938af46fbf25b1b51d5b4bb2bd9a2eead1b9c2f2b`, `9a2d70be70b70ef27036a47550bc0d549437df0c032a4e0237a217e4731e1aee`, and `619e0b9780ec204b9fbf6f48b2eb541c9d8a6f19a73f27d4d76d25828db7d369`. |
| Verify the Program drawcardlib fonts | `shasum -a 256 Program/TT0530m_.ttf Program/TT0127m_.ttf Program/TT0085m_.ttf Program/TT0298m_.ttf Program/TT0299m_.ttf Program/TT0300m_.ttf`; expect the hashes recorded in [docs/runtime-manifest.md](docs/runtime-manifest.md). |
| Verify the Program drawcardlib CardArt assets | `shasum -a 256 Program/CardArt/ManaSymbols.pic Program/CardArt/Expansion_Symbols.pic Program/CardArt/Watermarks.pic Program/CardArt/CardCounters.png Program/CardArt/Modern/Triggering.png Program/CardArt/Modern/CardOv_Nyx.png Program/CardArt/Planeswalker/LoyaltyBase.png Program/CardArt/Planeswalker/LoyaltyMinus.png Program/CardArt/Planeswalker/LoyaltyPlus.png Program/CardArt/Planeswalker/LoyaltyZero.png`; expect the hashes recorded in [docs/runtime-manifest.md](docs/runtime-manifest.md). |
| Verify the active FaceMaker state | Run `xxd -g1 -l 32 -s $((0x5f40)) FaceMaker.exe` and `xxd -g1 -l 32 -s $((0x5f40)) Program/FaceMaker.exe`; expect `6a 00 57 50 8b 4d 10 51 ff 75 04` at the start of each dump. Then run `shasum -a 256 FaceMaker.exe Program/FaceMaker.exe FaceMaker-nores.exe Program/FaceMaker-nores.exe` and keep the patched active hash distinct from the no-resolution helper hash. |
| Verify the ManalinkEh damage-prevention patches | Run `xxd -g1 -l 32 -s $((0x3bb035)) ManalinkEh.dll` and `xxd -g1 -l 32 -s $((0x381a25)) Program/ManalinkEh.dll`; expect `f6 05 90 f1 4e 00 04 0f 84 ae 00 00 00 e9 01 00` at the start of both dumps. Then run `xxd -g1 -l 5 -s $((0x44cb23)) ManalinkEh.dll`, `xxd -g1 -l 38 -s $((0x495a30)) ManalinkEh.dll`, `xxd -g1 -l 5 -s $((0x40f115)) Program/ManalinkEh.dll`, and `xxd -g1 -l 38 -s $((0x452c30)) Program/ManalinkEh.dll`; expect the generic `GAA_DAMAGE_PREVENTION*` cave bytes recorded in [docs/bugs/duel-freeze-damage-prevention.md](docs/bugs/duel-freeze-damage-prevention.md). Then run `shasum -a 256 ManalinkEh.dll Program/ManalinkEh.dll`; expect `68f2ba31f26f99edfb0944fe3fbc577ef0a42f9f6a6d7d44cb3aaa5f9b9cadd5` and `619ce5d3f80f4ac951418e8a1b2ec803b3b9aa0128e01b827e744b80e63962fc`. |
| Verify the ManalinkEh AI decision-time clamp patch | Run `xxd -g1 -l 11 -s $((0x40d0e1)) ManalinkEh.dll`, `xxd -g1 -l 24 -s $((0x495a60)) ManalinkEh.dll`, `xxd -g1 -l 11 -s $((0x3d2da1)) Program/ManalinkEh.dll`, and `xxd -g1 -l 24 -s $((0x452c60)) Program/ManalinkEh.dll`; expect the hook/cave bytes recorded in [docs/bugs/opponent-turn-ai-decision-time.md](docs/bugs/opponent-turn-ai-decision-time.md). `objdump -D -Mintel --start-address=0x0240dad0 --stop-address=0x0240db10 ManalinkEh.dll` plus `--start-address=0x02497060 --stop-address=0x02497090`, and matching Program ranges `0x023d3790..0x023d37d0` plus `0x02454060..0x02454090`, should show `_check_timer_for_ai_speculation` jumping to a cave that preserves values `1..270` and writes `mov ebx, 0x10e` for nonpositive or higher values. |
| Verify the ManalinkEh AI raw-mana snapshot patch | Run `xxd -g1 -l 5 -s $((0x40db84)) ManalinkEh.dll`, `xxd -g1 -l 59 -s $((0x495a90)) ManalinkEh.dll`, `xxd -g1 -l 5 -s $((0x3d3844)) Program/ManalinkEh.dll`, and `xxd -g1 -l 59 -s $((0x452c90)) Program/ManalinkEh.dll`; expect the hook/cave bytes recorded in [docs/bugs/ai-raw-mana-snapshot.md](docs/bugs/ai-raw-mana-snapshot.md). `objdump -D -Mintel --start-address=0x0240e580 --stop-address=0x0240e5c0 ManalinkEh.dll` plus `--start-address=0x02497090 --stop-address=0x024970d0`, and matching Program ranges `0x023d4240..0x023d4280` plus `0x02454090..0x024540d0`, should show `_ai_decision_phase` jumping to a cave that saves all 16 `raw_mana_available` dwords before replacing only the opponent row. |
| Verify the ManalinkEh Piranha Marsh trigger-target patch | Run `xxd -p -l 75 -s $((0x3fe7a0)) ManalinkEh.dll` and `xxd -p -l 75 -s $((0x3c4930)) Program/ManalinkEh.dll`; expect the root and Program bytes recorded in [docs/bugs/piranha-marsh-spell-chain-freeze.md](docs/bugs/piranha-marsh-spell-chain-freeze.md). `objdump -D -Mintel --start-address=0x023ff1a0 --stop-address=0x023ff1f0 ManalinkEh.dll` and matching Program range `0x023c5330..0x023c5380` should show `card_piranha_marsh` calling `_pick_player_duh`, then `_get_card_instance`, then `_lose_life`. |
| Verify the ManalinkEh Bojuka Bog trigger-target patch | Run `xxd -p -l 117 -s $((0x3f63e0)) ManalinkEh.dll` and `xxd -p -l 117 -s $((0x3bc630)) Program/ManalinkEh.dll`; expect the root and Program bytes recorded in [docs/bugs/ai-etb-player-target-spell-chain-freeze.md](docs/bugs/ai-etb-player-target-spell-chain-freeze.md). `objdump -D -Mintel --start-address=0x023f6de0 --stop-address=0x023f6e60 ManalinkEh.dll` and matching Program range `0x023bd030..0x023bd0b0` should show `card_bojuka_bog` calling `_pick_player_duh`, then `_get_card_instance`, then `_rfg_whole_graveyard`. |
| Verify the ManalinkEh generic AI player-target selector patch | Run `xxd -p -l 17 -s $((0x469583)) ManalinkEh.dll`, `xxd -p -l 44 -s $((0x495ad0)) ManalinkEh.dll`, `xxd -p -l 17 -s $((0x429453)) Program/ManalinkEh.dll`, and `xxd -p -l 44 -s $((0x452cd0)) Program/ManalinkEh.dll`; expect the hook/cave bytes recorded in [docs/bugs/ai-etb-player-target-spell-chain-freeze.md](docs/bugs/ai-etb-player-target-spell-chain-freeze.md). `objdump -D -Mintel --start-address=0x02469f83 --stop-address=0x02469fa8 ManalinkEh.dll` plus `--start-address=0x024970d0 --stop-address=0x02497100`, and matching Program ranges `0x02429e53..0x02429e78` plus `0x024540d0..0x02454100`, should show `_select_target_impl` jumping to a cave that writes `0` to `0x7a2fe4` for `TARGET_ZONE_PLAYERS` and otherwise calls `_sub_499050`. |
| Verify the Shandalar generic AI player-target selector patch | Run `xxd -p -l 18 -s $((0xcb16)) Shandalar.dll`, `xxd -p -l 64 -s $((0x1174920)) Shandalar.dll`, `xxd -p -l 18 -s $((0xcb16)) Program/Shandalar.dll`, and `xxd -p -l 64 -s $((0x1174920)) Program/Shandalar.dll`; expect the hook/cave bytes recorded in [docs/bugs/ai-etb-player-target-spell-chain-freeze.md](docs/bugs/ai-etb-player-target-spell-chain-freeze.md). `objdump -D -Mintel --start-address=0x0600d710 --stop-address=0x0600d750 Shandalar.dll` plus `--start-address=0x06d5d120 --stop-address=0x06d5d150` should show `Target::real_select_target()` jumping to a cave that writes `0` to `0x0094d46c` only when `[this+0x8] == AI` and `[this+0x14] == TARGET_ZONE_PLAYERS`, and otherwise calls the original selector. |
| Verify the ManalinkEh AI land CIP resolver stack-bypass patch | Run `xxd -p -l 10 -s $((0x429acf)) ManalinkEh.dll`, `xxd -p -l 5 -s $((0x3f63bd)) ManalinkEh.dll`, `xxd -p -l 5 -s $((0x3fe77d)) ManalinkEh.dll`, `xxd -p -l 247 -s $((0x495b00)) ManalinkEh.dll`, `xxd -p -l 10 -s $((0x3ec7cf)) Program/ManalinkEh.dll`, `xxd -p -l 5 -s $((0x3bc60d)) Program/ManalinkEh.dll`, `xxd -p -l 5 -s $((0x3c490d)) Program/ManalinkEh.dll`, and `xxd -p -l 247 -s $((0x452d00)) Program/ManalinkEh.dll`; expect the hook/restored-call/cave bytes recorded in [docs/bugs/ai-etb-player-target-spell-chain-freeze.md](docs/bugs/ai-etb-player-target-spell-chain-freeze.md). `objdump -D -Mintel --start-address=0x0242a4c0 --stop-address=0x0242a500 ManalinkEh.dll` plus `--start-address=0x02497100 --stop-address=0x02497200`, and matching Program ranges `0x023ed1c0..0x023ed200` plus `0x02454100..0x02454200`, should show `_resolve_trigger` jumping to a cave that handles non-speculating AI land `TRIGGER_COMES_INTO_PLAY` before stack insertion, otherwise falling back to the original resolver. |
| Verify the declared-attacker undo patch | Run `xxd -g1 -l 16 -s $((0x43c303-0x400000)) Magic.exe`, `xxd -g1 -l 16 -s $((0x43c303-0x400000)) Program/Magic.exe`, `xxd -g1 -l 80 -s $((0x459bc8-0x400000)) Magic.exe`, and `xxd -g1 -l 80 -s $((0x459bc8-0x400000)) Program/Magic.exe`; expect the hook to begin `e9 c0 d8 01 00 90 90 90 90 90 90 90 90` and the cave to begin `f7 46 08 04 00 00 00 0f 85 12 00 00 00`. Then run `shasum -a 256 Magic.exe Program/Magic.exe`; expect `93a40ce2c96aafee1d858a71ed69eb8c539aa9851796eb54b1af58f0bb97aba0` and `685669692634ec830fe228904e11b1b536bd4b20e52192863a6280c2dbff6b66`. |
| Verify the Magic coin-flip animation default patch and startup call-site guard | Run `xxd -g1 -l 10 -s $((0x5db1f)) Magic.exe` and `xxd -g1 -l 10 -s $((0x5db1f)) Program/Magic.exe`; expect `c7 05 5c 72 78 00 00 00 00 00`, which changes the missing-registry `ShowCoinFlips` default from `1` to `0`. Then run `xxd -g1 -l 16 -s $((0x694b7)) Magic.exe`, `xxd -g1 -l 16 -s $((0x694eb)) Magic.exe`, and the same two commands for `Program/Magic.exe`; expect the dumps to begin `6a 00`, proving the startup/mulligan `coin_flip()` call sites keep `show_dialog_if_animation_is_off = 0`. |
| Verify FaceMaker support files | `cmp -s Program/FaceData.txt Manalink3/Program/FaceData.txt && cmp -s Program/FaceButtons.txt Manalink3/Program/FaceButtons.txt && diff -qr Program/FaceArt Manalink3/Program/FaceArt` |
| Verify the active start-color config | `rg -n "^Window\\s*=" Shandalar.ini Program/Shandalar.ini`; both should be `Window = 2` unless a later test deliberately changes them. |
| If testing CrossOver bottle `MTG`, verify its copied install too | `rg -n "^Window\\s*=" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/drive_c/Shandalar/Shandalar.ini" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/drive_c/Shandalar/Program/Shandalar.ini"` |
| Verify the current `MTG` app-default desktop/version and duel-option settings | `sed -n '260,280p;790,825p;965,970p' "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/MTG/user.reg"`; expect `ShowCoinFlips=0` under `Software\\MicroProse\\Magic: The Gathering\\DuelOptions`, plus `FaceMaker.exe`, `Magic.exe`, and `Shandalar.exe` app-default `Version=win7`, desktop `Shandalar1440`, and `Shandalar1440=1440x1080` under `HKCU\Software\Wine\Explorer\Desktops`. |
| Verify the current `MTG` copied install state | `tools/verify-crossover-mtg-state.sh`; this is local-machine specific and does not launch the game. |
| Verify the fresh Win8 bottle test state | `ls -ld "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/Shandalar-Win8-Test/drive_c/Shandalar"` and `rg -n "ShandalarTall|Desktop" "/Users/mdmoll/Library/Application Support/CrossOver/Bottles/Shandalar-Win8-Test/user.reg"`; expect a `C:\Shandalar` copy and app-default desktop `ShandalarTall=1024x800`. |
| Search docs/scripts for stale paths | `rg -n -i --glob '*.txt' --glob '*.md' --glob '*.cmd' --glob '*.bat' --glob '*.ini' --glob '*.pl' -e 'https?://|www\.|[A-Z]:\\|c:\\|d:\\' .` |
| Review limited reorg history | `sed -n '1,220p' docs/reorganization.md` |
| Count files by top directory | `find . -type f | sed 's#^./##' | awk 'BEGIN{FS="/"} {print $1}' | sort | uniq -c | sort -nr | head -40` |
| Count extensions | `find . -type f | awk 'BEGIN{IGNORECASE=1} {n=$0; sub(/^.*\//,"",n); if (n !~ /\./) ext="[none]"; else {ext=n; sub(/^.*\./,"",ext); ext=tolower(ext)}; count[ext]++} END{for (e in count) print count[e], e}' | sort -nr | head -40` |
| Inspect `.tmp` runtime-looking assets before cleanup | `file Statwin/*.tmp Program/statwin/*.tmp && shasum -a 256 Statwin/*.tmp Program/statwin/*.tmp` |
| Create a disposable cleanup test copy | `tools/create-cleanup-test-copy.sh /private/tmp/shandalar-cleanup-test`; the default omits `.git` because launch tests do not need history. |
| Dry-run a Git handoff bundle if push auth is unavailable | `tools/create-git-handoff-bundle.sh --dry-run` |
| Recreate the current Git handoff bundle if push auth is unavailable | `tools/create-git-handoff-bundle.sh --replace` |
| Dry-run a binary patch package for patch-only planning | `tools/create-patch-package.sh --dry-run` |
| Recreate and apply-verify the current binary patch package | `tools/create-patch-package.sh --replace --verify-apply` |
| Print the manual gameplay evidence baseline | `tools/print-manual-gameplay-baseline.sh` |
| Record one manual gameplay field or result | `tools/record-manual-gameplay-result.sh --test D2 --result "Fail: froze at post-combat Done; screenshot /path/to/screenshot.png"` |
| Print the security scan evidence baseline | `tools/print-security-scan-baseline.sh` |
| Record scanner evidence after a real scan | `tools/record-security-scan-result.sh --confirmed-real-scan --path Shandalar.exe --scanner "Windows Defender" --version "VERSION" --date 2026-05-31 --result "Clean" --notes "MpCmdRun.exe custom scan completed"` |
| List branch delta for review or patch-only planning | `tools/list-branch-delta.sh` |
| Run the non-gameplay handoff verifier | `tools/verify-handoff-readiness.sh` |
| Verify bundle import in a disposable clone | `tools/verify-handoff-readiness.sh --verify-bundle-import` |
| Verify default handoff artifacts too | `tools/verify-handoff-readiness.sh --verify-bundle-import --verify-artifacts` |
| Run the strict final controlled-maintenance gates | `tools/verify-final-share-gates.sh`; this should still fail until the remaining manual gameplay evidence is complete. |
| Run automated share-readiness checks | `tools/verify-share-readiness.sh` |
| Build dry run | `cd src && make -n` |

## CrossOver/Wine Commands to Record

Use only when the tool and bottle are available. Always record the exact bottle,
working directory, command, exit code, and any visible UI/log result.

```sh
cd /Users/mdmoll/Shandalar/Shandalar

wine Shandalar.exe --help
wine Shandalar.exe

cd /Users/mdmoll/Shandalar/Shandalar/Program
wine Magic.exe

/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" "C:\Shandalar\Shandalar.exe"
/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" "C:\Shandalar\Magic.exe"
/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar" "C:\Shandalar\FaceMaker.exe"
/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar\Program" "C:\Shandalar\Program\Shandalar.exe"
/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle MTG --workdir "C:\Shandalar\Program" "C:\Shandalar\Program\Magic.exe"
/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle Shandalar-Win8-Test --workdir "C:\Shandalar" "C:\Shandalar\Shandalar.exe"
/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/wine --bottle Shandalar-Win8-Test --workdir "Y:\Shandalar\Shandalar" "Y:\Shandalar\Shandalar\Shandalar.exe"
```

If a launch opens a GUI and cannot be observed from automation, mark it "needs
manual visual test" rather than guessing.

For the start-color assertion, the strongest current smoke logs are
`/tmp/shandalar-repo-patched-sendkeys-cx.log`,
`/tmp/shandalar-win8test-cdrive-patched-sendkeys-cx.log`, and
`/tmp/shandalar-mtg-cdrive-patched-sendkeys-cx.log`, which used Wine `wscript`
SendKeys against the patched repo and C-drive binaries and got past the
post-color resource load point. Treat them as crash-point verification, not
full gameplay proof.

## Runtime Testing Policy

| Rule | Why |
| --- | --- |
| This is not primarily a Wine/CrossOver GUI-testing repo task unless the user explicitly says so. | The useful work is usually docs, source/static inspection, inventory, cleanup planning, and targeted bug analysis. |
| Use at most one bounded runtime attempt per relevant target by default. | Repeated relaunches and key-sequence experiments are expensive and fragile. |
| Do not add new SendKeys/AppleScript/xdotool helpers for exploratory UI navigation. | Existing helpers are narrow smoke-test evidence aids, not a gameplay automation framework. |
| Prefer one logged run, static inspection, and manual test checklists over trying to play the game through automation. | Wine/CrossOver focus behavior is not reliable enough to be the main proof path. |
| Record exact command, working directory, bottle/container, Windows version, log path, screenshot path, and visible result for every attempted launch. | Future readers need evidence, not impressions. |
| Keep native Windows evidence separate from Wine/CrossOver evidence. | Bottle behavior is not proof of native Windows behavior. |

See [docs/runtime-testing-policy.md](docs/runtime-testing-policy.md) for the
full stop conditions and evidence fields.
